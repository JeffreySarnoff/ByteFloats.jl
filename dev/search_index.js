var documenterSearchIndex = {"docs":
[{"category":"section","location":"technical_guide/#Technical-Guide","page":"Technical Guide","text":"How ByteFloats.jl works inside: the encoding, the projection engine, the oracle and its correctness protocol, the performance layers, and the verification doctrine that holds it all together. Read the User Guide first; this page assumes its vocabulary.","title":"Technical Guide"},{"category":"section","location":"technical_guide/#Layer-map","page":"Technical Guide","text":"Source files load in dependency order, each layer speaking only downward:\n\nlayer file provides\nformats formats.jl Binary{K,P,SGN,EXT}, the 120 named aliases, Group M queries\nspecs projspec.jl rounding/saturation singletons, ProjSpec{R,S}, the predefined spec grid\ndefaults defaults.jl settable session defaults (DefaultType, DefaultProjection, …) behind Refs; consumed via the speculation guard\ncodec decode_encode.jl decode (generated tables + bit-composed compute), encode, order keys, counting sort, Class, Next ops\nengine project.jl round_to_precision (mask-based Float64 core + generic core), saturate, project, project_interval\nops ops_scalar.jl result-kind protocol, apply_op, the operation registry, the spec register\ncompat juliacompat.jl the Base register: declarative op ⇒ Base-function map\noracle oracle.jl ω-semantics for all 52 operations\ntables tables.jl the pure-ρ result-table cache (unary/binary + bitwidth-gated ternary)\nkernels kernels.jl Shape-A gathers, Shape-B scalar/threaded loops, vmap\nblocks blocks.jl Block, block/scaled ops, exact reductions\npacked packed.jl sub-byte PackedVector storage\napprox approx.jl κ measurement/registry, conformance declaration\nrand rand.jl Random-API hooks: uniform-[0,1) floor projection, clamped normal","title":"Layer map"},{"category":"section","location":"technical_guide/#Encoding-and-decoding","page":"Technical Guide","text":"A value is its code point (UInt8). The bit layout is the draft's: sign (signed formats), biased exponent, trailing significand; one NaN at the negative-zero slot; no −0; ±Inf adjacent to the extremes in extended formats.\n\ndecode is a @generated constant-tuple lookup: per format, a 2^K-tuple of Float64 datums built once from the computational decode (so table and computation are correct by construction and asserted equivalent exhaustively). Constant inputs still fold — maxfinite_datum(T) is a compile-time constant — while runtime decode is a single indexed load (≈ 0.7 ns). The computational decode assembles the Float64 by bits (normalize with leading_zeros, place exponent and mantissa fields, reinterpret), valid because every datum's exponent sits deep inside Float64's normal range; Float64 is thereby the exact carrier for all datums, an invariant the suite checks against an independent big-float transliteration of the draft.\n\nOrdering runs on integer order keys: a sign-magnitude fold into UInt16, monotone with the total order, NaN mapped to typemax. Same-format TotalOrder, isless, and the numeric comparisons are key comparisons (~1 ns); since a format has at most 2^K + 1 distinct keys, vectors sort with an O(n) counting sort installed via Base.Sort.defalg (forward and reverse orderings; anything exotic falls back to the stock algorithm).","title":"Encoding and decoding"},{"category":"section","location":"technical_guide/#The-projection-engine","page":"Technical Guide","text":"project(fr, ρ, X; R, sticky) is the single write path into a code point:\n\nRoundToPrecision  →  Saturate  →  Encode\n\nRoundToPrecision produces a Rounded(kind, sign, S, Q) — an exact scaled significand S and exponent Q per the draft's Q = max(⌊log₂|X|⌋, 1−B) − P + 1. Two implementations, proven equivalent exhaustively:\n\nthe generic core (_rtp_core) works on any carrier (Float64, Float128, BigFloat) via exact power-of-two scaling and a fraction ν compared against the mode's decision points;\nthe mask-based Float64 core (_rtp_f64) extracts sign/exponent/significand fields directly, represents ν as a 128-bit fixed-point integer with an OR-mask sticky for bits shifted out, and evaluates every mode — including the stochastic ⌊ν·2^N⌋ comparisons and RNITE ties — as integer field tests. Specials, zeros, and (Convert-only-reachable) subnormal Float64 inputs bail to the generic core.\n\nSymbolic sticky is how enclosures talk to the engine: sticky ∈ {−1, 0, +1} declares the true value to be the carried value plus an infinitesimal of that sign. The engine folds it into every comparison; the delicate case — true value just below a representable dyadic — decrements into the previous binade and sets ν = 1⁻ (encoded in the mask core as an all-ones fixed-point fraction, which reproduces every predicate including the RNITE tie behavior). This is what lets directed modes land exactly on asymptotes: tanh → 1⁻ under TowardNegative projects to NextLessThan(1), automatically.\n\nSaturate classifies against the format's range with two integer comparisons per side, then maps the classification through the draft's twenty saturation rows to as-is / MaxFinite / MinFinite / ±Inf / NaN. The comparisons stay cheap because:\n\nthe extremal magnitude in canonical (S, Q) form is a constant-folded function of the type parameters;\nthe rounded value and the extremum share one lexicographic (Q, S) order (subnormals and the lowest normal binade share the same Q);\nsigned formats use sign–magnitude symmetry, and unsigned underflow is just the sign bit;\nan internal HUGEQ sentinel represents \"finite but astronomically large\" (the ν = 1⁻ image of an infinite endpoint), so directed SatNone clamps it to MaxFinite correctly.\n\nEncode is pure integer bit assembly, including the significand-carry renormalization and the subnormal/normal field split.","title":"The projection engine"},{"category":"section","location":"technical_guide/#The-oracle-and-the-result-kind-protocol","page":"Technical Guide","text":"Every operation's defined result is computed by ωeval, which returns one of five result kinds; apply_op fast-splits the common one and finishes the rest:\n\nkind meaning finished by\nFloat64 exact (specials; representable arithmetic) direct project\nFloat128 exact by width analysis direct project (Float128 carrier)\nStickyF wide-spread FMA/FAA tail: head value (Float64 or Float128) + tail sign direct project with sticky set — no allocation\nBigExactF exact at 2200-bit precision (wide-spread tail for Add; the near-impossible FAA distillation miss) project on BigFloat\nEnclose128F correctly-rounded Float128 bracket sticky agreement, MPFR fallback\nEncloseF MPFR directed enclosure f(prec), optional Float128 pre-filter fq, optional eager Float64 estimate yd three-stage: yd → fq → interval protocol\n\nTwo rigor classes govern every non-Float64 path, and their arguments are never mixed:\n\nClass R (unconditional) rests on two facts that hold without any accuracy assumption:\n\nWidth analysis. Sums of decoded datums are exactly representable in Float128 whenever operand bits + exponent spread fit 113 bits — checked in advance by integer exponent arithmetic (Add at ΔE ≤ 100, FMA ≤ 92, FAA span ≤ 98). Beyond the threshold, Add escalates to the 2200-bit path, while FMA/FAA take a non-allocating sticky-head shortcut: past that spread the smaller term is provably too small (by construction of the threshold) to affect anything but the tail direction of the larger one, so the result is StickyF(head, sign) — no BigFloat involved. FAA's three-term case runs a bounded Float128 2sum-distillation (Priest-style, ≤ 6 sweeps) to find that head/tail split directly; the residual BigExactF MPFR fallback exists only for the near-impossible case the distillation doesn't converge.\nIEEE correct rounding. Division and sqrt are correctly rounded at every binary width, by mandate. Divide/Recip therefore use the Float64 quotient directly: it is within half an ulp of truth, so it serves as EncloseF's eager yd estimate with no Float128 arithmetic on the path at all. Sqrt/RSqrt use the Float128 CR result: an inexact nearest-CR value q brackets the truth in the open interval (prevfloat(q), nextfloat(q)). Exactness itself is detected by an fma residual test.\n\nClass E (envelope-conditional). Faithful-but-not-CR evaluations stand in for an enclosure only inside a generous envelope, resolved by the two-sided sticky gate. Two stages, cheapest first:\n\nFloat64 stage: for operations whose Float64 libm is faithful (≤ 1 ulp ≈ 2⁻⁵² relative — the exp/log families, the hyperbolics, forward trig inside the |x| ≤ 10¹⁵ reduction window, atan, asinh, the softplus composition), the eager estimate yd carries envelope E = |yd|·2⁻⁴⁵, ≥ 2⁷ slacker than any faithful-libm error (measured margin on this machine: ≥ 2⁶·⁶).\nFloat128 stage: libquadmath's estimate y = fq() carries E = |y|·2⁻⁹⁰, at least 2¹⁸ slacker than any published libquadmath bound.\n\nIn both stages, if the sticky-projected endpoints agree, that code point is the answer; if not, the next stage (ultimately the MPFR ladder) decides. An envelope failure can therefore only cost speed, never correctness — unless both endpoints agree and the envelope is wrong, which the differential-build tests rule out empirically by building every standard table twice (Float128-first and pure-MPFR) and byte-diffing, and which the exhaustive oracle cross-check re-verifies per operation against the 3072-bit protocol.\n\nThe interval protocol (project_interval) is the termination backbone: evaluate the MPFR enclosure at precision p; if lo == hi the value is exactly representable — project it; otherwise project lo with sticky = +1 and hi with sticky = −1; agreement (projection is monotone at fixed R) yields the answer; disagreement doubles p, clamped to the maxprec ceiling (default 4096 bits, honored exactly even when it is not a power-of-two multiple of the 256-bit start). Termination requires that the enclosure not be chasing a value the grid can actually hit — which is why the π-scaled operations carry Niven peels: for dyadic arguments, tan(πr) takes rational values only at the quarter-integers (±1) and atan2/π only on the diagonals (±¼, ±¾); those cases are answered exactly before any enclosure is built, and Niven's theorem proves the peel set complete.","title":"The oracle and the result-kind protocol"},{"category":"section","location":"technical_guide/#Tables-and-kernels","page":"Technical Guide","text":"For pure specs, unary and binary operations are finite functions — 256 or 65 536 entries — so the kernel layer materializes them once per (op, formats, ρ) into a locked cache (Dict{TableKey, Memory{UInt8}}, double-checked locking, builds outside the lock) and serves every later array call as a gather: Shape-A, one load per element, measured 0.27 ns/elem unary and 0.5 ns/elem binary. Tables are built through the scalar path, so they inherit its bit-exactness; the suite asserts table ≡ scalar over every entry.\n\nTernary (FMA, FAA, Clamp) is a finite function too — 2^(K1+K2+K3) entries — but that count spans four orders of magnitude across the 3–8 bitwidth range (512 B at K=3, 16 MiB at K=8), so one policy doesn't fit the whole range. A separate TernaryKey → TernaryEntry cache (_ternary_table_for, in tables.jl) tiers by Σ bitwidth:\n\nEager (≤ 18 bits, all K ≤ 6 combinations, ≤ 256 KiB): builds and caches on the first array call.\nAdaptive (≤ 21 bits, the K = 7 band, up to 2 MiB): accumulates a per-signature element count across calls and builds only once a signature has processed enough elements to amortize the build; a byte-bounded LRU eviction (TERNARY_CACHE_BYTES) guards against many hot signatures coexisting.\nCompute (K = 8, 16+ MiB — a table is never worth it): vmap! runs Shape-B, the fully specialized scalar pipeline per element, optionally split across Threads.@threads for long enough arrays (each ternary draw is independent under a fixed, non-stochastic ρ, so lanes cannot interact).\n\nEvery ternary table entry, eager or adaptive, is still built through the scalar path — the tiering changes when/whether the cache exists, never what it contains. Stochastic calls of any arity always take Shape-B, with the RNG resolved once per array rather than per element.","title":"Tables and kernels"},{"category":"section","location":"technical_guide/#Blocks:-exactness-without-a-superaccumulator","page":"Technical Guide","text":"blockdecode produces each lane's scale × element exactly (≤ 17-bit significands in Float64). Reductions then apply span filters: one integer pass over lane exponents decides whether the whole sum (or dot product, with ≤ 33-bit lane products) is exactly representable in Float128; if so, a plain Float128 accumulation is the exact answer; if not, an exact big-float accumulation takes over. Either way there is exactly one projection, at the end. ωBlockProject follows the draft's special rows for the scale (NaN, 0, ±Inf) and divides each element result by the scale through its own cheapest-first CR-bracket / enclosure cascade (exact Float64 quotient → CR Float128 → bracket/pre-filter → MPFR interval), mirroring the scalar quotient group's rigor arguments.","title":"Blocks: exactness without a superaccumulator"},{"category":"section","location":"technical_guide/#The-κ-registry-and-conformance","page":"Technical Guide","text":"κ is the maximum code-point distance (along the total order) between an implementation's result and the defined result, over inputs with finite defined results; any mismatch on non-finite defined results makes κ = NaN. Because inputs are enumerable, register_approx! measures κ exhaustively at registration and rejects understatement — a declared bound is a verified property, not a promise. conformance() assembles the declaration live from the operation registry, the table cache, and the approximation registry.","title":"The κ registry and conformance"},{"category":"section","location":"technical_guide/#Verification-doctrine","page":"Technical Guide","text":"The value sets are small enough that sampling is never necessary, so the suite enumerates — ≈ 8.9 M assertions in all:\n\nformats against an independent draft transliteration (14 679);\nordering over all 2.5 M same-format pairs plus Next-op edge tables (7.6 M);\nevery unary operation on every input against a 3072-bit protocol run; divide and the ternaries exhaustively;\nstochastic R-sweeps with directed-asymptote pins;\ntable ≡ scalar over every entry, including the ternary tiers (eager and adaptive) against the scalar path;\nthe sticky-head FMA/FAA escalation against the MPFR reference across every rounding-mode family and adversarial cancellation cases;\nblocks against a from-scratch reference composition;\nFloat128 carrier ≡ Float64 carrier, and Float128-first ≡ MPFR differential builds;\nthe mask rounding core ≡ the generic core over datums and reachable sums/products at boundary stochastic budgets N ∈ {45, 60};\npacked round-trips, and κ/conformance behavior.\n\nDeterministic specialization regressions (concrete inferred return types at the public entry points, zero warm-path allocation) stand in for timing assertions.","title":"Verification doctrine"},{"category":"section","location":"technical_guide/#Benchmark-doctrine","page":"Technical Guide","text":"Recorded after two measurement post-mortems: a benchmark closure over any non-const global measures Julia's dispatch machinery, not the code under test — and it distorts ratios, not just absolutes, because dispatch cost varies with call shape (a dynamic keyword call costs ~1 µs; a dynamic positional call far less; six unresolved interior sites cost six times one).\n\nThe rules the shipped benchmarking/benchmarking.jl enforces structurally:\n\nformat types enter as type parameters, never as globals;\noperands come from untimed setup;\nfunctions retrieved reflectively pass through argument barriers to specialize;\na preflight aborts the run if warm scalar paths allocate — including the wide-spread FMA/FAA sticky-head path.\n\nThe table-build section reports both the cold build (cache evicted per sample) and the steady-state warm cache hit, since callers amortize the former through the latter. Vary one binding per variant; verify specialization before believing a number.","title":"Benchmark doctrine"},{"category":"section","location":"technical_guide/#Deliberate-limitations","page":"Technical Guide","text":"No implicit cross-format arithmetic (promotion is to Float64, explicitly). No in-place packed arithmetic. Threading is opt-in and narrow: only the untabled ternary (K = 8) compute kernel threads, and only above a size cutoff and when Threads.nthreads() > 1; every other kernel is single-threaded. Irrational/Rational inputs to Convert are rejected rather than double-rounded silently. The Float128 machinery never changes results — disabling it (ByteFloats_Float128=disable) is a tested no-op semantically.","title":"Deliberate limitations"},{"category":"section","location":"new_operations/#Adding-Operations","page":"Adding Operations","text":"How to add a new operation to ByteFloats.jl so that it inherits everything the existing catalog has: bit-exact defined results, the full projection-mode vocabulary, generated scalar/array/block surfaces, table caching, exhaustive test coverage, benchmark pickup, and a truthful conformance declaration. Read the Technical Guide first — this page assumes its vocabulary (the result-kind protocol, the two rigor classes, the interval protocol, the yd envelope stage).\n\nThe architecture is registry-driven: for the supported arities, most of the surface is generated. Adding an operation is mostly (1) one registry line and (2) one rigorous ωeval method — plus the duties that keep the package's guarantees true. Every subsection below states the duties explicitly; they are not optional.","title":"Adding Operations"},{"category":"section","location":"new_operations/#Universal-duties-(every-new-operation)","page":"Adding Operations","text":"Special rows are spec, not optimization. Write out the NaN/±Inf/zero rows explicitly in ωeval, in draft style, before any general evaluation. Mark any behavior the draft text under-determines with [interp].\nResult-kind protocol. ωeval must return one of Float64 | Float128 | StickyF | BigExactF | EncloseF | Enclose128F, with Float64/Float128 reserved for provably exact results and StickyF for exact-head-plus-tail-direction results whose soundness bound is discharged at the emitting site (see the FMA/FAA wide-spread escalations). Keep the return union narrow (ideally {Float64, EncloseF}) — a wide union defeats inference and costs allocations (measured: a 4-type union turned a 50 ns op into 240 ns with 2 allocations).\nTermination (the Niven duty). project_interval terminates only if the enclosure is not chasing a value the projection grid can actually hit. Any input whose exact result is a representable dyadic must be peeled or detected before an enclosure is built. For transcendental ops this is usually a finite set of special rows (prove completeness, as the π-family does with Niven's theorem); for algebraic ops it is an exactness test.\nEnvelope justification. If you supply an eager yd estimate, you owe an error bound: the true value must lie within |yd|·2⁻⁴⁵ (_F64_RELEXP). Acceptable bases, strongest first: IEEE-CR hardware ops (÷, sqrt: ≤ ½ ulp, unconditional); short compositions of CR ops (≤ ~1.5 ulp); faithful libm (≤ 1 ulp, an empirical-but-published claim); short faithful compositions with condition number ≤ 1 (≤ ~3 ulp). Measure it: sample your estimator against ≥ 256-bit MPFR truth over the op's domain, including the treacherous regions (near zeros of the result, domain boundaries), and demand ≥ 2⁶ slack under 2⁻⁴⁵. The same duty applies to a Float128 fq estimator against its 2⁻⁹⁰ envelope. Beware argument-error amplification: an estimate computed as f(g(x)) where g rounds (e.g. sin(π·r) in Float64) can be catastrophically worse near zeros of f than a native result-domain evaluation (sinpi(r)) — measure the composition you actually ship.\nLadder validity. The MPFR closure must return a genuine directed enclosure. Whole-expression setrounding is valid only when the composition is monotone in every intermediate (state the argument in a comment, as Softplus does); otherwise round each step in the correct direction explicitly (as _mpfr_rsqrt does for the anti-monotone 1/√x).\nGates. Run the full suite. The unary exhaustive ×hp gate and the coverage check pick up registry ops automatically; extend the hand-curated lists where applicable (monotonicity ops, binary exhaustive sweeps). Then regenerate the benchmark report — bench_scalar_ops iterates the registry lists, so your op appears in all four operand-class tables automatically; add a _SAFE_DOMAINS entry in benchmarking/benchmarking.jl if the op is argument-restricted, or the safe-args table will use the oracle-derived fallback pool.","title":"Universal duties (every new operation)"},{"category":"section","location":"new_operations/#Scalar-operations","page":"Adding Operations","text":"The registry lives in src/ops_scalar.jl. Adding a name to _UNARY_OPS / _BINARY_OPS / _TERNARY_OPS causes, with no further work:\n\nregistration in OP_REGISTRY (group :B/:C/:A per the loop defaults — override with an explicit register_op! if the default group is wrong);\nthe generated spec-register method Op(fr, ρ, operands…; rng, R) and the same-format convenience Op(x…);\ngenerated array methods routing through vmap (Shape-A tables for pure ρ — always at arity ≤ 2, bitwidth-gated at arity 3 — Shape-B scalar/threaded loops otherwise);\ngenerated BlockOp and ScaledOp variants (src/blocks.jl);\nthe export (src/ByteFloats.jl loops the registry);\ntable cacheability, conformance listing, and test-harness coverage.\n\nWhat is not generated is the mathematics: ωeval in src/oracle.jl.","title":"Scalar operations"},{"category":"section","location":"new_operations/#Adding-a-unary-operation","page":"Adding Operations","text":"Worked example: Cbrt (cube root), chosen because it exercises the hardest duty — exactness detection for an algebraic op.\n\nStep 1 — registry (src/ops_scalar.jl): append :Cbrt to _UNARY_OPS. The loop registers it as group :B with arity 1.\n\nStep 2 — ω-semantics (src/oracle.jl), with every duty discharged:\n\n# Exact-cube detection (termination duty): x = ±m·2^(3k) with m an odd perfect\n# cube ⇒ cbrt(x) is a Float64-exact dyadic that project_interval must never\n# chase. Oracle-path cost is irrelevant; correctness of the peel is everything.\nfunction _exact_cbrt(x::Float64)\n    fr, e = frexp(abs(x))               # |x| = fr·2^e, fr ∈ [0.5, 1)\n    m = Int64(ldexp(fr, 53)); e -= 53   # |x| = m·2^e exactly\n    tz = trailing_zeros(m); m >>= tz; e += tz\n    e % 3 == 0 || return nothing\n    r = round(Int64, cbrt(Float64(m)))\n    for c in max(r - 1, 1):(r + 1)      # cbrt is faithful ⇒ true root ∈ r ± 1\n        Int128(c)^3 == Int128(m) && return flipsign(ldexp(Float64(c), e ÷ 3), x)\n    end\n    nothing\nend\n\nfunction ωeval(::Val{:Cbrt}, x::Float64)\n    isnan(x) && return NaN                      # special rows first: they are spec\n    isinf(x) && return x                        # cbrt(±∞) = ±∞\n    iszero(x) && return 0.0\n    c = _exact_cbrt(x)\n    c === nothing || return c                   # exact dyadic result — peeled\n    # yd: Float64 cbrt is faithful (≤ 1 ulp ≈ 2⁻⁵²; measure it) ⇒ ≥ 2⁷ slack\n    # under the 2⁻⁴⁵ envelope. fq: libquadmath cbrtq under the 2⁻⁹⁰ envelope.\n    # Ladder: cbrt is monotone ⇒ whole-expression directed rounding encloses.\n    _encl1(cbrt, x; fq=() -> cbrt(Float128(x)), yd=cbrt(x))\nend\n\nStep 3 — optional surface: a Base veneer (Base.cbrt(x::Binary) = Cbrt(x) next to the others in ops_scalar.jl), a docstring, and — since cbrt is monotone — its symbol in the test suite's monotonicity list.\n\nStep 4 — validate: envelope measurement for cbrt (include tiny and huge x; cbrt has no dangerous regions — the result is never near zero except at zero, which is peeled), then the full suite: the exhaustive ×hp gate now checks Cbrt on every code point of the reference format under four modes against the 3072-bit protocol, automatically.","title":"Adding a unary operation"},{"category":"section","location":"new_operations/#Adding-a-binary-operation","page":"Adding Operations","text":"Worked example: LogAddExp — log(eˣ + eʸ) — chosen because it exercises composed estimators and the ladder-monotonicity argument.\n\nStep 1 — registry: append :LogAddExp to _BINARY_OPS (default group :C; the special five arithmetic names get :A).\n\nStep 2 — ω-semantics. One generic evaluator serves all three precisions — Float64 (yd), Float128 (fq), and BigFloat (the ladder):\n\n# Stable form: h + log1p(exp(l − h)), h = max, l = min. Every step is monotone\n# nondecreasing in (x, y), so whole-expression directed rounding in _mpfr2 is a\n# valid enclosure (the same argument Softplus records).\n_logaddexp(a, b) = (h = max(a, b); l = min(a, b); h + log1p(exp(l - h)))\n\nfunction ωeval(::Val{:LogAddExp}, x::Float64, y::Float64)\n    (isnan(x) | isnan(y)) && return NaN\n    (x == Inf || y == Inf) && return Inf        # ∞ dominates\n    x == -Inf && return y                       # log(0 + eʸ) = y, exactly\n    y == -Inf && return x\n    # yd: two faithful libm calls plus additions, condition ≤ 1 throughout\n    # (result ≥ max(x, y)) ⇒ ≤ ~3 ulp ≈ 2⁻⁵⁰ — 2⁵ slack; measure it, including\n    # x ≈ y (where log1p(exp(0)) dominates) and widely separated operands.\n    _encl2(_logaddexp, x, y;\n           fq=() -> _logaddexp(Float128(x), Float128(y)),\n           yd=_logaddexp(x, y))\nend\n\nTermination note: can logaddexp(x, y) be exactly dyadic for dyadic operands with the special rows peeled? log(eˣ + eʸ) = d requires eˣ + eʸ = e^d — by Lindemann–Weierstrass this forces relations that dyadic arguments cannot satisfy except on the peeled rows, so no further peel is needed; record that argument in a comment. When you cannot prove such a statement for your op, hunt for exact cases the way _exact_cbrt does.\n\nStep 3 — tests: binary ops are not in the unary exhaustive gate; add a divide-style exhaustive sweep (256×256 operand pairs against a ladder-only reference, a handful of modes) to the suite, and an envelope-sanity loop entry if you introduced a new libm dependence.","title":"Adding a binary operation"},{"category":"section","location":"new_operations/#Adding-a-ternary-operation","page":"Adding Operations","text":"Worked example: FMS — fused multiply-subtract, x·y − z — chosen because the cheapest correct implementation is delegation, and delegation is a first-class technique here.\n\nStep 1 — registry: append :FMS to _TERNARY_OPS (group :A by the loop). The generated array method routes through the same ternary bitwidth policy every other ternary op uses (tables.jl's _ternary_table_for): small operand formats table (eagerly or adaptively), K = 8 runs the — optionally threaded — Shape-B scalar loop, and stochastic ρ always draws per element. No op-specific work needed; the policy keys on formats and ρ, not on which op it is.\n\nStep 2 — ω-semantics by delegation to FMA's already-verified analysis (width thresholds, _twosum exactness, wide-spread fallback):\n\n# x·y − z ≡ FMA(x, y, −z). Negation of z follows the Subtract convention:\n# preserve NaN, and map −0 to +0 so the single-zero datum model is respected.\nωeval(::Val{:FMS}, x::Float64, y::Float64, z::Float64) =\n    ωeval(Val(:FMA), x, y, isnan(z) ? z : (iszero(z) ? 0.0 : -z))\n\nDelegation inherits FMA's result kinds, its exactness thresholds, and its test pedigree — but verify the sign algebra of your reduction on the special rows (here: FMS(∞, 1, ∞) must be NaN, which the delegation produces because FMA(∞, 1, −∞) is ∞ − ∞). Add the delegated op to the ternary exhaustive sweep in the suite.","title":"Adding a ternary operation"},{"category":"section","location":"new_operations/#Adding-a-quaternary-operation","page":"Adding Operations","text":"Arity 4 is not yet plumbed — the generation loops, kernels, and block layer handle arities 1–3. Adding a quaternary op is therefore two tasks: extend the plumbing (once), then add the op. Worked example: FMMA — x·y + z·w, a two-term dot product.\n\nPlumbing (one-time), four sites:\n\nsrc/ops_scalar.jl — register manually after the arity loops (register_op!(:FMMA, 4, :A)), and add an op.arity == 4 branch to the spec-register generation loop, mirroring the ternary branch with a fourth operand:\nelse # arity 4\n    @eval begin\n        @inline function $name(fr::Type{<:Binary}, ρ::ProjSpec,\n                               x::Binary, y::Binary, z::Binary, w::Binary;\n                               rng::MaybeRNG=nothing, R::Union{Nothing,Int}=nothing)\n            apply_op($V(), fr, ρ, _drawR(ρ, rng, R),\n                     decode(x), decode(y), decode(z), decode(w))\n        end\n        @inline $name(x::T, y::T, z::T, w::T; kw...) where {T<:Binary} =\n            $name(T, default_projspec(T), x, y, z, w; kw...)\n    end\nend\n(apply_op and ωeval are already variadic — no change needed there.)\nsrc/kernels.jl — a four-array vmap! method. The plain Shape-B loop (copy the body of the ternary method's compute branch, add operand D) is enough to get correct results; the ternary method's table-tiering and threading are a bitwidth-driven optimization layered on top (tables.jl's _ternary_table_for and its LRU cache), not required plumbing — extend to arity 4 only if a table win at that arity is worth the 2^(4K) growth. Also add an arity-4 branch in the registry-generated array-surface loop and the matching rng-threading overload (mirroring the three-array one) so stochastic ρ dispatches correctly.\nsrc/blocks.jl — either an arity-4 branch in the Block/Scaled generation loop (four operand blocks, four blockdecodes) or an explicit skip (op.arity > 3 && continue) if a block form is not wanted; be deliberate.\nsrc/approx.jl — conformance_report prints arities with for a in 1:3; widen to 1:4.\n\nThe op itself follows the FAA/FMA width-analysis playbook exactly:\n\nconst _DE_FMMA = 92     # two exact ≤17-bit products: 18 + ΔE ≤ 113, margin 3\n\nfunction ωeval(::Val{:FMMA}, x::Float64, y::Float64, z::Float64, w::Float64)\n    (isnan(x) | isnan(y) | isnan(z) | isnan(w)) && return NaN\n    (((iszero(x) && isinf(y)) || (isinf(x) && iszero(y))) ||\n     ((iszero(z) && isinf(w)) || (isinf(z) && iszero(w)))) && return NaN\n    p = x * y; q = z * w                          # exact: ≤8-bit significands\n    if isinf(p) || isinf(q)\n        (isinf(p) && isinf(q) && p != q) && return NaN\n        return isinf(p) ? p : q\n    end\n    s, e = _twosum(p, q)\n    e == 0.0 && return iszero(s) ? 0.0 : s        # exact in Float64\n    (_f128() && !iszero(p) && !iszero(q) && _expdiff(p, q) <= _DE_FMMA) &&\n        return Float128(p) + Float128(q)          # exact by width\n    BigExactF(() -> setprecision(() ->\n        BigFloat(x) * BigFloat(y) + BigFloat(z) * BigFloat(w), BigFloat, _BIGP))\nend\n\nTesting duty is heavier at arity 4: the full cross-product is 2³² tuples, so the suite entry must be sampled (seeded, ladder-referenced, ≥ 10⁵ tuples across several modes) plus hand-picked exhaustion of the special-row algebra and the width-threshold boundary (ΔE ∈ {91, 92, 93} constructions). Document the sampling in the test, since it breaks the suite's \"enumerate, never sample\" norm — that exception must be visible, not silent.","title":"Adding a quaternary operation"},{"category":"section","location":"new_operations/#Block-operations","page":"Adding Operations","text":"The block layer (src/blocks.jl) implements the draft's elementwise schema once — blockdecode → ω-op lanewise → blockproject — and generates every registry op's BlockOp/ScaledOp from it. The reductions are hand-written on a second shared pattern. Which subsection you need depends on whether your operation fits one of those two patterns.","title":"Block operations"},{"category":"section","location":"new_operations/#Adding-a-scaled-block-operation","page":"Adding Operations","text":"If the scalar operation is (or can be) a registry op, the scaled form is free: registering :Cbrt above already produced ScaledCbrt(fr, ρ, s, x) and BlockCbrt(fr, ρ, b, sr) with the draft's §5.4 semantics — decode scale × element exactly per lane, apply the ω-op, BlockProject against the result scale through the division cascade. Prefer this route; it is the package's non-divergence mechanism.\n\nWrite a scaled op by hand only when it is not an elementwise image of a registry op. The primitive to compose with is _bp_element(fr, ρ, R, res, Sdat) — it owns the draft's scale-special rows (NaN/0/±Inf scale) and the entire division-by-scale rigor cascade. Example, a scaled affine s₁·x + s₂·y projected against a unit result scale:\n\nfunction ScaledAffine(fr::Type{<:Binary}, ρ::ProjSpec,\n                      s1::Binary, x::Binary, s2::Binary, y::Binary;\n                      rng::MaybeRNG=nothing)\n    a = ωeval(Val(:Multiply), decode(s1), decode(x))::Float64   # exact lanes\n    b = ωeval(Val(:Multiply), decode(s2), decode(y))::Float64\n    _bp_element(fr, ρ, _drawR(ρ, rng, nothing), ωeval(Val(:Add), a, b), 1.0)\nend\n\nThe ::Float64 assertions are load-bearing: lane decodes are exact by width (≤ 17-bit significands), and the assertion turns that analysis into a runtime check. The Add result may be any kind in the protocol — _bp_element finishes all of them. Dividing by a scale of 1.0 is the identity fast path.","title":"Adding a scaled block operation"},{"category":"section","location":"new_operations/#Adding-an-elementwise-block-operation","page":"Adding Operations","text":"Same schema, whole-block granularity. The three obligations: decode operand blocks with blockdecode (exact), evaluate lanewise with ωeval (never with ad-hoc Float64 math), and finish with blockproject against the supplied result scale (never with per-lane project — the scale-division rows belong to blockproject). Example — BlockRelu, an elementwise max(x, 0) that reuses the verified MaximumNumber semantics:\n\n\"\"\"BlockRelu(fr, ρ, b, sr): lanewise max(xᵢ, 0) — NaN lanes propagate NaN per\nMaximumNumber's number-preferring rows only when both operands are NaN, i.e.\nnever here; state your lane semantics this explicitly for any new op.\"\"\"\nfunction BlockRelu(fr::Type{<:Binary}, ρ::ProjSpec, b::Block{B}, sr::Binary;\n                   rng::MaybeRNG=nothing) where {B}\n    X = blockdecode(b)\n    Z = ntuple(i -> ωeval(Val(:MaximumNumber), X[i], 0.0), Val(B))\n    blockproject(fr, ρ, sr, Z; rng)\nend\n\nAdd the name to the block-surface export list and to conformance()'s blocknames extension vector so the declaration stays truthful. Test against a from-scratch reference composition (the suite's existing block tests show the pattern) — element format × scale format × B, exhaustively where feasible.","title":"Adding an elementwise block operation"},{"category":"section","location":"new_operations/#Adding-a-reductive-block-operation","page":"Adding Operations","text":"Reductions do not fit the elementwise schema; they follow the second pattern, visible in BlockReduceAdd/BlockDotProduct: (1) resolve the ∞/NaN fold algebra on Float64 classifications first; (2) an integer span filter decides whether the whole reduction is exactly representable in Float128 — if so, plain Float128 accumulation is the exact answer; (3) otherwise a BigExactF exact accumulator at provably sufficient precision; (4) exactly one projection, at the very end, via _finish. Never round intermediates.\n\nWorked example — BlockSumOfSquares, Σ xᵢ²:\n\n\"\"\"BlockSumOfSquares(fr, ρ, b): project(Σ decode-laneᵢ²) — one rounding total.\nFold algebra: any NaN lane ⇒ NaN; else any ±Inf lane ⇒ +Inf (squares cannot\ncancel — no opposite-infinity NaN case exists, unlike ReduceAdd); else exact.\"\"\"\nfunction BlockSumOfSquares(fr::Type{<:Binary}, ρ::ProjSpec, b::Block{B};\n                           rng::MaybeRNG=nothing, R::Union{Nothing,Int}=nothing) where {B}\n    X = blockdecode(b)\n    res = if any(isnan, X)\n        NaN\n    elseif any(isinf, X)\n        Inf\n    elseif all(iszero, X)\n        0.0\n    else\n        # span filter: lanes carry ≤17-bit significands ⇒ squares ≤34 bits and\n        # a square's exponent is 2·(lane exponent); the sum is exact in Float128\n        # when 34 + 2·span + ⌈log₂B⌉ + 1 ≤ 113  ⇒  2·span + ⌈log₂B⌉ ≤ 78.\n        if _f128() && 2 * _expspan(X) + _log2ceil(B) <= 78\n            acc = Float128(0)\n            for v in X\n                q = Float128(v)\n                acc += q * q                      # exact squares, exact sum\n            end\n            acc\n        else\n            BigExactF(() -> setprecision(BigFloat, _REDPREC) do\n                acc = BigFloat(0)\n                for v in X\n                    acc += BigFloat(v)^2          # exact at _REDPREC\n                end\n                acc\n            end)\n        end\n    end\n    _finish(fr, ρ, _drawR(ρ, rng, R), res)\nend\n\nThe two lines that demand your care in any new reduction are the fold algebra (derive it from the draft's reduce definition on the extended reals — here the absence of an opposite-infinities NaN case is a theorem about squares, stated in the docstring) and the span-filter inequality (derive it from operand widths, write the derivation in the comment, and add boundary-condition tests at the threshold, exactly as the suite does for the existing _DE_* constants). Everything else — the exact accumulator, the single _finish, stochastic R plumbing — is pattern.","title":"Adding a reductive block operation"},{"category":"section","location":"new_operations/#After-any-addition:-the-closing-checklist","page":"Adding Operations","text":"Full test suite green, including your new sweep entries.\nEnvelope measurements recorded for any new yd/fq estimator.\nBenchmark report regenerated; _SAFE_DOMAINS entry added if the op is argument-restricted; sanity-check the op's row in the safe-args table.\nDocstring on the public name; [interp] markers where you interpreted.\nconformance_report() shows the op (automatic for registry ops; manual block additions extend blocknames).","title":"After any addition: the closing checklist"},{"category":"section","location":"technical_examples/#Technical-Examples","page":"Technical Examples","text":"Internals-level recipes: introspecting the pipeline, verifying custom code against the oracle, measuring κ, and benchmarking without fooling yourself. Outputs are captured from real sessions. Several examples use unexported internals (ByteFloats.round_to_precision, ByteFloats.project, …); those are stable enough to learn from but are not covered by semantic-versioning guarantees.","title":"Technical Examples"},{"category":"section","location":"technical_examples/#Basic","page":"Technical Examples","text":"","title":"Basic"},{"category":"section","location":"technical_examples/#Watching-RoundToPrecision-work","page":"Technical Examples","text":"round_to_precision returns the draft's exact (sign, S, Q) decomposition before saturation ever sees it:\n\nusing ByteFloats\nusing ByteFloats: round_to_precision\n\nr = round_to_precision(4, 8, NearestTiesToEven(), 2.30078125, 0, 0)   # P=4, bias=8\n(r.sign, r.S, r.Q, r.S * 2.0^r.Q)\n\n(1, 9, -2, 2.25)          # S·2^Q = 9/4: the nearest P=4 value","title":"Watching RoundToPrecision work"},{"category":"section","location":"technical_examples/#Symbolic-sticky:-projecting-\"just-below\"-a-number","page":"Technical Examples","text":"The engine accepts sticky ∈ {-1, 0, +1} meaning \"the true value is the carrier plus an infinitesimal of this sign\". This is how enclosure endpoints and asymptotes (tanh → 1⁻) are projected exactly:\n\nusing ByteFloats: project\nproject(Binary8p4se, ProjSpec(TowardNegative(), SatNone()), 1.0; sticky=-1)\n\nBinary8p4se(0.9375 ≡ 0x3f)     # == NextLessThan(1.0): the engine crossed the binade","title":"Symbolic sticky: projecting \"just below\" a number"},{"category":"section","location":"technical_examples/#Tables-are-the-scalar-path,-memoized","page":"Technical Examples","text":"using ByteFloats: get_table\nempty_tables!()\ntbl = get_table(:Exp, Binary8p4se, Binary8p4se, RNE_SatNone)\n(tbl[Int(0x45) + 1],\n codepoint(Exp(Binary8p4se, RNE_SatNone, rawvalue(Binary8p4se, 0x45))),\n table_bytes())\n\n(0x52, 0x52, 256)              # table entry ≡ scalar result; one 256-byte table cached","title":"Tables are the scalar path, memoized"},{"category":"section","location":"technical_examples/#Order-keys","page":"Technical Examples","text":"Ordering is integer arithmetic: a sign-magnitude fold, monotone with the total order, NaN at the top. This is what comparisons and the O(n) counting sort run on:\n\nusing ByteFloats: order_key\n[(v, order_key(v)) for v in (Binary8p4se(-1.0), Binary8p4se(0.0),\n                             Binary8p4se(1.0), Binary8p4se(NaN))]\n\n-1.0 → 64      0.0 → 129      1.0 → 193      NaN → 65535","title":"Order keys"},{"category":"section","location":"technical_examples/#General-AI","page":"Technical Examples","text":"","title":"General AI"},{"category":"section","location":"technical_examples/#Ranking-safety:-an-exhaustive-monotonicity-audit-of-score-conversion","page":"Technical Examples","text":"Decision and ranking systems consume scores through order. If scores are produced in one format and compared in another, the conversion must be monotone — and formats are small enough to prove it by enumeration rather than trust it. Walk every Binary8p3se datum in total order and check that its Binary8p4se image never goes backward on the integer order keys:\n\nusing ByteFloats\nusing ByteFloats: order_key\n\nfunction monotone_conversion(::Type{From}, ::Type{To}) where {From,To}\n    prev = nothing\n    for v in sort(From.(0x00:UInt8(2^bitwidth(From) - 1)))\n        isnan(decode(v)) && continue\n        g = Convert(To, RNE_SatNone, v)\n        prev !== nothing && order_key(g) < order_key(prev) && return false\n        prev = g\n    end\n    true\nend\nmonotone_conversion(Binary8p3se, Binary8p4se)\n\ntrue\n\nRun this for every (From, To, ρ) triple your system actually uses; it is a few hundred integer comparisons per triple. A ranking pipeline whose conversions all pass this audit cannot invert a preference by changing formats — a guarantee no amount of spot-testing provides.","title":"Ranking safety: an exhaustive monotonicity audit of score conversion"},{"category":"section","location":"technical_examples/#κ-safe-decision-margins-for-approximate-evaluators","page":"Technical Examples","text":"Search under a compute budget often wants a cheap, approximate evaluation function. The κ registry turns \"how approximate?\" into a measured code-point bound — and code-point bounds compose into a decision rule: if two defined evaluations differ by more than 2κ code points along the total order, the approximate evaluator cannot invert their comparison. Verify the rule exhaustively for a κ = 2 evaluator:\n\nusing ByteFloats: order_key\n\nfast(x) = (r = Exp(Binary8p4se, RNE_SatNone, x);\n           isfinite(decode(r)) ? NextGreaterThan(NextGreaterThan(r)) : r)\nκ, exhaustive = measure_kappa(fast, :Exp, Binary8p4se, (Binary8p4se,), RNE_SatNone)\n\nfunction margin_audit(fast, κ)\n    codes = [rawvalue(Binary8p4se, UInt8(c)) for c in 0:255]\n    safe = violations = 0\n    for a in codes, b in codes\n        da, db = Exp(Binary8p4se, RNE_SatNone, a), Exp(Binary8p4se, RNE_SatNone, b)\n        (isnan(decode(da)) || isnan(decode(db))) && continue\n        codedistance(da, db) > 2κ || continue\n        safe += 1\n        (order_key(fast(a)) < order_key(fast(b))) ==\n            (order_key(da) < order_key(db)) || (violations += 1)\n    end\n    (safe, violations)\nend\n(κ, exhaustive, margin_audit(fast, κ)...)\n\n(2.0, true, 49522, 0)\n\n49,522 operand pairs clear the 2κ margin, and the approximate evaluator agrees with the defined ordering on every one of them — zero violations, exhaustively. Inside the margin, comparisons are genuinely undecidable at this κ; a search can treat sub-margin comparisons as ties to expand, or escalate those few nodes to the exact evaluator. Either way the pruning is provably sound, with κ measured at registration rather than promised.","title":"κ-safe decision margins for approximate evaluators"},{"category":"section","location":"technical_examples/#Machine-Learning","page":"Technical Examples","text":"","title":"Machine Learning"},{"category":"section","location":"technical_examples/#Verifying-a-quantizer-exhaustively-against-an-independent-reference","page":"Technical Examples","text":"Formats are small enough that \"test on a few points\" is never necessary. Here every in-range datum of Binary8p3se is projected into Binary8p4se and checked against a brute-force nearest search under 256-bit arithmetic (distance agreement; the draft's tie rule is the projection engine's job and is pinned elsewhere in the suite):\n\nusing ByteFloats\n\nfunction ref_nearest_distance(::Type{T}, x) where {T}\n    fins = [v for v in (rawvalue(T, UInt8(c)) for c in 0:255) if isfinite(decode(v))]\n    minimum(abs(setprecision(() -> BigFloat(decode(v)) - BigFloat(x), BigFloat, 256))\n            for v in fins)\nend\n\nfunction verify_quantizer()\n    ok = true\n    for c in 0x00:0xff\n        x = decode(rawvalue(Binary8p3se, c))\n        (isfinite(x) && abs(x) <= decode(MaxFiniteOf(Binary8p4se))) || continue\n        got = Convert(Binary8p4se, RNE_SatNone, x)\n        ok &= abs(decode(got) - x) == Float64(ref_nearest_distance(Binary8p4se, x))\n    end\n    ok\nend\nverify_quantizer()\n\ntrue\n\nThis pattern — enumerate the inputs, compare against an independently written reference — is how the entire package is tested, and it is available to your quantization code at trivial cost.","title":"Verifying a quantizer exhaustively against an independent reference"},{"category":"section","location":"technical_examples/#The-κ-workflow,-including-the-part-where-it-says-no","page":"Technical Examples","text":"Register an approximate kernel and the registry measures it; understate the bound and it refuses:\n\nstep2(x) = (r = Exp(Binary8p4se, RNE_SatNone, x);\n            isfinite(decode(r)) ? NextGreaterThan(NextGreaterThan(r)) : r)\n\nmeasure_kappa(step2, :Exp, Binary8p4se, (Binary8p4se,), RNE_SatNone)\n\n(2.0, true)                # κ = 2 code points, verified over all 256 inputs\n\nregister_approx!(:cheater, :Exp, Binary8p4se, (Binary8p4se,), RNE_SatNone, step2; κ=1)\n\nERROR: ArgumentError: declared κ = 1.0 understates measured κ = 2.0 — registration rejected","title":"The κ workflow, including the part where it says no"},{"category":"section","location":"technical_examples/#Exporting-the-conformance-declaration","page":"Technical Examples","text":"d = conformance_dict()          # plain nested Dict{String,Any}\n(d[\"package\"], length(d[\"formats\"]), length(d[\"operations\"]),\n [s[\"op\"] for s in d[\"cached_specializations\"]])\n\nSerialize d with any JSON/TOML writer to attach a machine-readable conformance statement to experiment artifacts.","title":"Exporting the conformance declaration"},{"category":"section","location":"technical_examples/#Deep-Learning","page":"Technical Examples","text":"","title":"Deep Learning"},{"category":"section","location":"technical_examples/#Proving-your-fused-kernel-exact:-BlockDotProduct-vs-512-bit-truth","page":"Technical Examples","text":"The block layer promises one projection with exact lane arithmetic. Trust, then verify — against big-float truth, over random blocks with mixed scales and an honest special-value mix:\n\nusing ByteFloats, Random\n\nrng = Xoshiro(5)\nmk() = Block(Binary8p1uf(2.0^rand(rng, -3:3)),\n             ntuple(_ -> rawvalue(Binary8p4se, UInt8(rand(rng, 0:255))), 32))\n\nfunction verify_dots(trials)\n    for _ in 1:trials\n        bx, by = mk(), mk()\n        lx = [decode(bx.s) * decode(v) for v in bx.x]\n        ly = [decode(by.s) * decode(v) for v in by.x]\n        (any(!isfinite, lx) || any(!isfinite, ly)) && continue\n        truth = setprecision(() -> sum(BigFloat(lx[i]) * BigFloat(ly[i]) for i in 1:32),\n                             BigFloat, 512)\n        got = BlockDotProduct(Binary8p4se, RNE_SatNone, bx, by)\n        ref = setprecision(() -> ByteFloats.project(Binary8p4se, RNE_SatNone, truth), BigFloat, 512)\n        codepoint(got) == codepoint(ref) || return false\n    end\n    true\nend\nverify_dots(200)\n\ntrue\n\nThe same shape verifies any custom fused kernel you write: compose the truth in BigFloat, project once, compare code points.","title":"Proving your fused kernel exact: BlockDotProduct vs 512-bit truth"},{"category":"section","location":"technical_examples/#Stochastic-rounding,-audited:-the-full-R-sweep","page":"Technical Examples","text":"Every stochastic projection is a deterministic function of the draw R, so its distribution is checkable exactly. For x = 2 + 3/64 in Binary8p4se the fraction is ν = 3/16 of an ulp, so StochasticA{4} must round up for exactly 3 of the 16 draws:\n\nσ4 = RSA_SatNone(4)                  # ≡ ProjSpec(StochasticA{4}(), SatNone())\nx = 2.0 + 3/64\ncount(decode(ByteFloats.project(Binary8p4se, σ4, x; R)) == 2.25 for R in 0:15)\n\n3\n\nSweeping R like this turns \"is my stochastic pipeline unbiased?\" from a statistical question into an exhaustive one — the pattern the shipped test suite uses.","title":"Stochastic rounding, audited: the full-R sweep"},{"category":"section","location":"technical_examples/#An-accelerator-style-activation,-κ-measured:-hard-tanh-vs-Tanh","page":"Technical Examples","text":"Hardware activation units often ship piecewise approximations. The κ machinery makes the substitution honest: measure the approximation's worst code-point deviation from the defined activation, exhaustively, and register it under that measured bound. Hard-tanh — clamp(x, −1, 1) — as a stand-in for Tanh:\n\none4 = Binary8p4se(1.0)\nhardtanh(x) = Clamp(Binary8p4se, RNE_SatNone, x, Negate(one4), one4)\n\nκ, exhaustive = measure_kappa(hardtanh, :Tanh, Binary8p4se, (Binary8p4se,), RNE_SatNone)\n(κ, exhaustive)\n\n(4.0, true)                # worst deviation: 4 code points, verified on all 256 inputs\n\nWhere is it worst? At x = 1.0: hard-tanh returns 1.0 while the defined tanh(1.0) is 0.75 — four code points along the total order. The registration round-trip, including the conformance surface:\n\nimpl = register_approx!(:hardtanh_act, :Tanh, Binary8p4se, (Binary8p4se,),\n                        RNE_SatNone, hardtanh; κ=4)\n(kappa(:hardtanh_act), kappa_measured(impl), :hardtanh_act in list_approx())\n\n(4.0, 4.0, true)           # declared = measured; conformance_report() now lists it\n\n(unregister_approx!(:hardtanh_act) removes it.) Declaring κ=3 would be rejected — understatement is impossible by construction. The decision of whether a κ = 4 activation is acceptable now belongs to your accuracy budget, not to hope: combined with the margin rule from the General AI section, any two pre-activations whose defined Tanh images sit more than 8 code points apart keep their order under hard-tanh, provably.","title":"An accelerator-style activation, κ-measured: hard-tanh vs Tanh"},{"category":"section","location":"technical_examples/#Benchmarking-without-measuring-the-dispatcher","page":"Technical Examples","text":"The package's benchmark doctrine, in one snippet (needs the benchmarking/ environment for Chairmarks). Format types enter as type parameters; operands come from Chairmarks' untimed setup; and if you retrieve functions reflectively (getfield(ByteFloats, op)), pass them through an argument barrier so they specialize:\n\nusing Chairmarks, ByteFloats, Random\nusing Statistics: median\n\nfunction bench_add(::Type{T}) where {T<:Binary}          # T: type parameter, not a global\n    pool = [rawvalue(T, rand(UInt8)) for _ in 1:4096]\n    @be (rand(pool), rand(pool)) (t -> Add(T, RNE_SatNone, t[1], t[2]))(_)\nend\n\nb = bench_add(Binary8p4se)\n(round(median(b).time * 1e9; digits=1), median(b).allocs)\n\n(16.3, 0.0)          # ns per full scalar Add, zero allocations\n\nwarning: The 60× trap\nThe same call with T read from a non-const global measures ~1 µs — Julia's dynamic keyword dispatch, not this package. Two project post-mortems trace to exactly this mistake; the shipped benchmarking/benchmarking.jl asserts specialization (zero warm-path allocation) before it believes any number, and so should yours.","title":"Benchmarking without measuring the dispatcher"},{"category":"section","location":"cheat_sheet/#Cheat-Sheet","page":"Cheat Sheet","text":"A one-page reference for the ByteFloats.jl operations you are most likely to write. For explanations and semantics, see the User Guide; for implementation details, see the Technical Guide.","title":"Cheat Sheet"},{"category":"section","location":"cheat_sheet/#Start-here","page":"Cheat Sheet","text":"using ByteFloats\nusing Random: Xoshiro\n\nT = Binary8p4se\nρ = RNE_SatNone\n\nx = T(1.6)\ny = T(0.25)\nz = Add(T, ρ, x, y)\n\nThe explicit operation shape is always:\n\nOp(result_format, projection_spec, operands...; rng, R)\n\nFor same-format operands under the default projection, ordinary Julia syntax is available:\n\nz == x + y\nexp(x)\nfma(x, y, z)","title":"Start here"},{"category":"section","location":"cheat_sheet/#Format-names","page":"Cheat Sheet","text":"Binary K p P (s|u) (e|f)\n       │   │  │     └─ extended (Inf) or finite domain\n       │   │  └────── signed or unsigned\n       │   └───────── significand precision, including the implicit bit\n       └───────────── total bitwidth, 3 through 8\n\nExamples:\n\nName Meaning\nBinary8p4se 8-bit, precision 4, signed, extended\nBinary8p4sf 8-bit, precision 4, signed, finite\nBinary6p3ue 6-bit, precision 3, unsigned, extended\nBinary5p5uf 5-bit, precision 5, unsigned, finite\n\nThe parametric spelling is equivalent:\n\nBinary8p4se === Binary{8, 4, true, true}\n\nUseful format queries:\n\nbitwidth(T)       # K\nprecision(T)      # P\nissigned(T)\nisextended(T)\nexpbias(T)\nexpbitwidth(T)\ntrailingsigbits(T)\n\nMaxFiniteOf(T)\nMinFiniteOf(T)\nMinPositiveOf(T)\nMinNormalOf(T)\nMaxSubnormalOf(T)\n\nEvery query above also takes a value instead of a type — bitwidth(x) ≡ bitwidth(typeof(x)) — and folds to the same constant:\n\nx = Binary8p4se(1.6)\nbitwidth(x)       # 8\nMaxFiniteOf(x)    # Binary8p4se(224.0 ≡ 0x7e)","title":"Format names"},{"category":"section","location":"cheat_sheet/#Values-and-code-points","page":"Cheat Sheet","text":"x = T(1.6)                    # numeric value: project into T\nx = Convert(T, ρ, 1.6)       # explicit projection\n\nc = codepoint(x)              # UInt8 code point\nx = T(c)                      # UInt8 means validated CODE POINT\nx = rawvalue(T, c)            # unchecked code-point construction\n\nd = decode(x)                 # exact Float64 datum\n\nwarning: `UInt8` means code point\nT(0x02) constructs code point 0x02; T(2) projects the numeric value two. Use Convert when intent should be unmistakable.\n\nRandom values (uniform [0,1) floor-projected; normal round-to-nearest, tails clamped to MaxFinite; randn signed formats only):\n\nrand(T); rand(T, dims); rand!(A)      # uniform on [0, 1)\nrandn(T); randn(T, dims); randn!(A)   # standard normal, always finite\nrand(Xoshiro(1), T)                   # explicit rng as usual\n\nrand(rng, T;  projection=RTP_SatNone)   # scalar forms: land under any ProjSpec\nrandn(rng, T; projection=RTZ_SatFinite) # (stochastic ρ draws R from same rng)\n\nClassification and stepping:\n\nisnan(x); isinf(x); isfinite(x); iszero(x)\nsignbit(x); issubnormal(x)\nClass(x)\nNextGreaterThan(x); NextLessThan(x)\nnextfloat(x); prevfloat(x)\nTotalOrder(x, y)","title":"Values and code points"},{"category":"section","location":"cheat_sheet/#Projection-specifications","page":"Cheat Sheet","text":"A projection specification is (rounding mode, saturation mode):\n\nρ = ProjSpec(TowardPositive(), SatFinite())\nroundingmode(ρ)\nsaturationmode(ρ)","title":"Projection specifications"},{"category":"section","location":"cheat_sheet/#Deterministic-projections","page":"Cheat Sheet","text":"Rounding SatFinite SatPropagate SatNone\nnearest, ties even RNE_SatFinite RNE_SatPropagate RNE_SatNone\nnearest, ties away RNA_SatFinite RNA_SatPropagate RNA_SatNone\ntoward +∞ RTP_SatFinite RTP_SatPropagate RTP_SatNone\ntoward −∞ RTN_SatFinite RTN_SatPropagate RTN_SatNone\ntoward zero RTZ_SatFinite RTZ_SatPropagate RTZ_SatNone\nround to odd RTO_SatFinite RTO_SatPropagate RTO_SatNone\n\nRNE_SatNone is the package-wide default. Saturation modes mean:\n\nMode Out-of-range behavior\nSatFinite clamp everything to the finite range\nSatPropagate preserve representable infinities; clamp other overflow\nSatNone apply the draft's domain-, signedness-, and direction-dependent rows","title":"Deterministic projections"},{"category":"section","location":"cheat_sheet/#Stochastic-projections","page":"Cheat Sheet","text":"Constructors are grouped by stochastic variant:\n\nVariant SatFinite SatPropagate SatNone\nA RSA_SatFinite(N) RSA_SatPropagate(N) RSA_SatNone(N)\nB RSB_SatFinite(N) RSB_SatPropagate(N) RSB_SatNone(N)\nC RSC_SatFinite(N) RSC_SatPropagate(N) RSC_SatNone(N)\n\nN is the random-bit budget, 1 ≤ N ≤ 60. Omitting it uses N = 8.\n\nσ = RSA_SatNone(8)\n\nAdd(T, σ, x, y; rng=Xoshiro(1))  # reproducible stream\nAdd(T, σ, x, y; R=17)            # exact draw, ideal for tests\n\nisstochastic(σ)                   # true\nnrandbits(σ)                      # 8\n\nFor an N-bit mode, explicit R must be in 0:(2^N - 1).","title":"Stochastic projections"},{"category":"section","location":"cheat_sheet/#Session-defaults","page":"Cheat Sheet","text":"Read with DefaultX(), set with DefaultX!(v):\n\nDefaultType()            # Binary8p2se     DefaultType!(Binary8p4se)\nDefaultReturnType()      # Binary8p2se     DefaultReturnType!(Binary8p3se)\nDefaultAccumulatorType() # binary32        DefaultAccumulatorType!(binary64)\nDefaultRoundingMode()    # NearestTiesToEven()\nDefaultSaturationMode()  # SatNone()\nDefaultProjection()      # RNE_SatNone\nDefaultRNG()             # Xoshiro         DefaultRNG!(Xoshiro(42))\nDefaultRbits()           # 8               DefaultRbits!(16)\n\nSetting a rounding/saturation component rebuilds DefaultProjection; setting DefaultProjection! directly updates both components. Always: DefaultProjection() === ProjSpec(DefaultRoundingMode(), DefaultSaturationMode()).\n\nThe convenience methods (a + b, Exp(x), T(2.1)) do consult DefaultProjection, so changing it changes their results globally. They read it through a speculation guard: allocation-free while the default holds its initial value, one dispatch per call after you change it. Explicit forms (Add(T, ρ, x, y)) are unaffected by the session default.\n\nConsume a default via the combinators — never by computing on a bare DefaultX() read. No dispatch while the default is unchanged, one barrier dispatch after a change; zero-alloc when f's result type doesn't depend on the default (a default-typed result boxes once at escape):\n\nwith_default_type((T, x) -> T(x), 1.5)          # Binary8p2se(1.5 ≡ 0x41)\nwith_default_projection((ρ, x, y) -> Add(T, ρ, x, y), x, y)\nwith_default_returntype(f, args...)              # f(DefaultReturnType(), args...)\nwith_default_accumulatortype(f, args...)","title":"Session defaults"},{"category":"section","location":"cheat_sheet/#Scalar-operation-catalog","page":"Cheat Sheet","text":"# explicit result format and projection\nAdd(T, ρ, x, y)\nFMA(T, ρ, x, y, z)\nConvert(T, ρ, external_value)\n\n# same-format default-projection convenience\nAdd(x, y)\nFMA(x, y, z)\n\nArity Operations\nUnary Abs, Negate, Sqrt, RSqrt, Recip, Exp, Exp2, ExpMinusOne, Log, Log2, LogOnePlus, Softplus, Sin, Cos, Tan, ArcSin, ArcCos, ArcTan, Sinh, Cosh, Tanh, ArcSinh, ArcCosh, ArcTanh, SinPi, CosPi, TanPi, ArcSinPi, ArcCosPi, ArcTanPi\nBinary CopySign, Add, Subtract, Multiply, Divide, Hypot, ArcTan2, ArcTan2Pi, Maximum, Minimum, MaximumNumber, MinimumNumber, MaximumMagnitude, MinimumMagnitude, MaximumMagnitudeNumber, MinimumMagnitudeNumber, MinimumFinite, MaximumFinite\nTernary FMA, FAA, Clamp\nConversion Convert\n\nCommon Base spellings under RNE_SatNone:\n\nx + y; x - y; x * y; x / y\n-x; abs(x); inv(x); sqrt(x)\nexp(x); exp2(x); expm1(x)\nlog(x); log2(x); log1p(x)\nsin(x); cos(x); tan(x)\nasin(x); acos(x); atan(x); atan(y, x)\nsinh(x); cosh(x); tanh(x)\nmin(x, y); max(x, y); clamp(x, y, z)\nfma(x, y, z); muladd(x, y, z)","title":"Scalar operation catalog"},{"category":"section","location":"cheat_sheet/#Arrays","page":"Cheat Sheet","text":"Every registered operation has elementwise array methods:\n\nA = T.([1.0, 1.5, 2.0])\nB = T.([0.25, 0.5, 0.75])\n\nC = Add(T, ρ, A, B)\nE = Exp(T, ρ, A)\nF = FMA(T, ρ, A, B, C)\n\nC = vmap(:Add, T, ρ, A, B)\nvmap!(C, Val(:Add), T, ρ, A, B)\n\nOperands and destination must have matching axes. Deterministic unary/binary operations use cached result tables; affordable ternary signatures may also use tables. Stochastic operations compute each element and consume one draw per projection.\n\ntable_bytes()\nempty_tables!()","title":"Arrays"},{"category":"section","location":"cheat_sheet/#Blocks-and-scaled-operations","page":"Cheat Sheet","text":"FS = Binary8p1uf\nFE = Binary8p4se\n\nb = Block(FS(4.0), FE(1.5), FE(-0.75), FE(2.0), FE(0.5))\n\nblocksize(b)\nscaleformat(b)\nelemformat(b)\n\nConvertFromBlock(T, ρ, b)\nBlockReduceAdd(T, ρ, b)\nBlockReduceMultiply(T, ρ, b)\nBlockDotProduct(T, ρ, b, b)\n\nEvery scalar operation except Convert has generated block and scaled forms:\n\nBlockAdd(T, ρ, b1, b2, result_scale)\nBlockExp(T, ρ, b, result_scale)\n\nScaledAdd(T, ρ, scale1, x1, scale2, x2)\nScaledExp(T, ρ, scale, x)\n\nQuantize against a supplied or automatically selected scale:\n\nConvertToBlock(FS, FE, ρ, values_tuple, scale)\nConvertToBlockMaxAbsFinite(FS, FE, scale_ρ, element_ρ, values_tuple)\n\nBlockVector stores many equal-shape blocks in a structure-of-arrays layout.","title":"Blocks and scaled operations"},{"category":"section","location":"cheat_sheet/#Packed-storage","page":"Cheat Sheet","text":"v = Binary5p2se.([0.5, 1.0, 1.5, 2.0])\npv = PackedVector(v)\n\npv[2]\npv[2] = Binary5p2se(0.75)\ncollect(pv)\n\nout = vmap(:Exp, Binary5p2se, ρ, pv)\n\nPackedVector stores each code point in exactly bitwidth(F) bits. Computation unpacks tiles internally; packed arithmetic is deliberately not in-place.","title":"Packed storage"},{"category":"section","location":"cheat_sheet/#Conformance-and-approximations","page":"Cheat Sheet","text":"conformance()\nconformance_dict()\nconformance_report()\ndraft_revision()\n\nκ, exhaustive = measure_kappa(fn, :Exp, T, (T,), ρ)\nregister_approx!(:fast_exp, :Exp, T, (T,), ρ, fn; κ)\nimpl = approx(:fast_exp)\nkappa(impl)\nkappa_measured(impl)\nlist_approx()\nunregister_approx!(:fast_exp)\n\nApproximate implementations are never substituted into the default API.","title":"Conformance and approximations"},{"category":"section","location":"cheat_sheet/#Common-traps","page":"Cheat Sheet","text":"Trap Correct pattern\nTreating UInt8 as a number T(Int(c)) for a numeric integer; T(c::UInt8) for a code point\nSilently mixing byte-float formats Convert explicitly: Convert(T, ρ, x)\nAssuming SatNone always clamps Choose SatFinite when clamping is required\nExpecting IEEE division-by-zero P3109 semantics here define x / 0 → NaN\nExpecting negative zero Every format has one zero; T(-0.0) === zero(T)\nPassing a Rational Convert explicitly to an exact supported carrier or knowingly to Float64\nReproducibility with stochastic rounding Supply a seeded rng, or an explicit R in tests\nUsing rawvalue on unchecked input Prefer validated T(c::UInt8) outside kernels","title":"Common traps"},{"category":"section","location":"cheat_sheet/#Performance-checklist","page":"Cheat Sheet","text":"Keep format types and projection specs in const bindings, function arguments, or type parameters.\nPut code using runtime-selected formats behind a function barrier.\nExpect the first deterministic array call for a specialization to build a table; benchmark warm calls separately.\nUse PackedVector when storage bandwidth matters more than direct byte access.\nUse table_bytes() to inspect cache footprint and empty_tables!() to reset it.\nDo not replace explicit projection semantics with @fastmath.\n\nFor worked applications, continue to User Examples. For correctness and benchmark methodology, continue to Technical Examples.","title":"Performance checklist"},{"category":"section","location":"internal_reference/#Internal-Reference","page":"Internal Reference","text":"Unexported machinery documented in-source and referenced by the Technical Guide and Technical Examples. Public exported names are in the External Reference.","title":"Internal Reference"},{"category":"constant","location":"internal_reference/#ByteFloats.DEFAULT_RBITS","page":"Internal Reference","text":"Default random-bit budget used by no-argument stochastic projection constructors.\n\n\n\n\n\n","title":"ByteFloats.DEFAULT_RBITS"},{"category":"type","location":"internal_reference/#ByteFloats.MaybeR","page":"Internal Reference","text":"An explicit stochastic draw R, or nothing to draw one from the rng.\n\n\n\n\n\n","title":"ByteFloats.MaybeR"},{"category":"constant","location":"internal_reference/#ByteFloats.NAN_ORDER_KEY","page":"Internal Reference","text":"The order key reserved for the single NaN — above every finite key and ±Inf.\n\n\n\n\n\n","title":"ByteFloats.NAN_ORDER_KEY"},{"category":"constant","location":"internal_reference/#ByteFloats.TERNARY_ADAPTIVE_BITS","page":"Internal Reference","text":"K1+K2+K3 up to which a ternary table may be built adaptively once the signature has processed TERNARY_BUILD_ELEMS[] elements (default 21 bits = 2 MiB — the K=7 band). Above this, ternary ops always run the compute kernel.\n\n\n\n\n\n","title":"ByteFloats.TERNARY_ADAPTIVE_BITS"},{"category":"constant","location":"internal_reference/#ByteFloats.TERNARY_BUILD_ELEMS","page":"Internal Reference","text":"Cumulative element count at which an adaptive-band signature earns its table.\n\n\n\n\n\n","title":"ByteFloats.TERNARY_BUILD_ELEMS"},{"category":"constant","location":"internal_reference/#ByteFloats.TERNARY_CACHE_BYTES","page":"Internal Reference","text":"Byte budget for the ternary cache; least-recently-used tables evict first.\n\n\n\n\n\n","title":"ByteFloats.TERNARY_CACHE_BYTES"},{"category":"constant","location":"internal_reference/#ByteFloats.TERNARY_EAGER_BITS","page":"Internal Reference","text":"K1+K2+K3 up to which a ternary table is built eagerly on first array call (default 18 bits = 256 KiB — covers every all-K≤6 signature).\n\n\n\n\n\n","title":"ByteFloats.TERNARY_EAGER_BITS"},{"category":"constant","location":"internal_reference/#ByteFloats.THREADED_KERNELS","page":"Internal Reference","text":"Master switch for threaded ternary compute loops.\n\n\n\n\n\n","title":"ByteFloats.THREADED_KERNELS"},{"category":"constant","location":"internal_reference/#ByteFloats.THREAD_MIN_ELEMS","page":"Internal Reference","text":"Minimum element count before the pure-ρ ternary compute loop threads.\n\n\n\n\n\n","title":"ByteFloats.THREAD_MIN_ELEMS"},{"category":"type","location":"internal_reference/#ByteFloats.BigExactF","page":"Internal Reference","text":"Deferred exact result: f() returns the value as an exact BigFloat.\n\n\n\n\n\n","title":"ByteFloats.BigExactF"},{"category":"type","location":"internal_reference/#ByteFloats.Enclose128F","page":"Internal Reference","text":"Correctly-rounded Float128 bracket (plan Site C, Class R): IEEE mandates correct rounding for Float128 /, sqrt, fma, so a nearest-CR result of a value known inexact brackets the truth in the open interval (lo, hi) = (prevfloat(q), nextfloat(q)) with no envelope assumption. f is the MPFR fallback for (near-impossible at P ≤ 8) grid-straddling brackets.\n\n\n\n\n\n","title":"ByteFloats.Enclose128F"},{"category":"type","location":"internal_reference/#ByteFloats.EncloseF","page":"Internal Reference","text":"Deferred enclosure, resolved by _finish through up to three stages, cheapest first:\n\nyd — an eager Float64 estimate (NaN ⇒ absent) whose libm evaluation is faithful (≤ 1 ulp); the true value is taken to lie within |yd|·2^-45 (_F64_RELEXP), ≥ 2^7 slacker than any faithful-libm error.\nfq — a zero-argument Float128 estimator (plan Site D, Class E; nothing ⇒ absent): true value within |y|·2^-90 of y = fq() (_F128_RELEXP), a bound ≥ 2^18 slacker than any published libquadmath transcendental error, discharged empirically by the differential-build tests.\nf(prec) — the rigorous MPFR ladder: directed (lo, hi)::NTuple{2,BigFloat} strictly bracketing the true value.\n\nEach estimate stage returns only when the two-sided sticky projection of its envelope agrees on a single code point; any disagreement (or an absent/degenerate estimate) falls through to the next stage. Stage 3 always decides.\n\n\n\n\n\n","title":"ByteFloats.EncloseF"},{"category":"type","location":"internal_reference/#ByteFloats.Rounded","page":"Internal Reference","text":"Result of ωRoundToPrecision in canonical integer form: a kind tag plus sign · S · 2^Q for finite results (design §5.2). The saturation stage consumes this without ever re-touching the carrier value.\n\n\n\n\n\n","title":"ByteFloats.Rounded"},{"category":"type","location":"internal_reference/#ByteFloats.StickyF","page":"Internal Reference","text":"Sticky-head exact result (wide-spread tail, Float128 revision follow-on): the true value is v + sgn·ε for an infinitesimal ε > 0 — v carries every bit the projection can consume and sgn ∈ {-1,+1} the direction of the neglected tail. Sound because v is either exactly on a rounding threshold of the target grid (where sticky decides, for every mode including stochastic sub-grids) or strictly farther from the nearest threshold than the tail magnitude; the emitting sites in oracle.jl discharge that bound (operand significands ≤ 17 bits, target grids ≤ P−1+N ≤ 67 fractional bits, spreads > the DE* thresholds). Non-allocating: replaces the BigFloat escalation for FMA/FAA.\n\n\n\n\n\n","title":"ByteFloats.StickyF"},{"category":"type","location":"internal_reference/#ByteFloats.TableKey","page":"Internal Reference","text":"Value key for the table cache: op + format parameters + ρ parameters (all values, so the cache itself is type-stable and non-allocating to query).\n\n\n\n\n\n","title":"ByteFloats.TableKey"},{"category":"type","location":"internal_reference/#ByteFloats.TernaryKey","page":"Internal Reference","text":"Value key for a ternary table: op + result + three operand formats + ρ.\n\n\n\n\n\n","title":"ByteFloats.TernaryKey"},{"category":"method","location":"internal_reference/#Core.Type-Union{Tuple{UInt8}, Tuple{EXT}, Tuple{SGN}, Tuple{P}, Tuple{K}} where {K, P, SGN, EXT}","page":"Internal Reference","text":"(T::Type{<:Binary})(c::UInt8) -> T\n\nConstruct a value from its code point (validated: for K < 8, c must be < 2^K or an ArgumentError is thrown; the range check folds away at K = 8 and for constant codes). Binary5p3sf(0x08) == Binary5p3sf(1.0).\n\nUInt8 is the only argument type with code-point semantics — every other Real (including other Integers) constructs by projecting the numeric value, and Convert is numeric for all integers: Binary8p4se(0x02) is code point 2 (the second-smallest subnormal), while Binary8p4se(2) is the number 2.0. rawvalue(T, c) remains the unchecked kernel-internal route.\n\n\n\n\n\n","title":"Core.Type"},{"category":"method","location":"internal_reference/#ByteFloats._bp_element-Tuple{Type{<:Binary}, ProjSpec, Int64, Any, Float64}","page":"Internal Reference","text":"One element of ωBlockProject: the draft's S-special rows, then ωDivide + ωProject.\n\nFast-path cascade after the special rows, ordered cheapest-first by result kind: exact Float64 quotient → CR Float128 quotient (exact or one-ulp bracket) → CR-divided Enclose128F bracket → fq-composition envelope (2^-89) — each resolved by the two-sided sticky gate; any miss falls through to the rigorous MPFR interval at the bottom.\n\n\n\n\n\n","title":"ByteFloats._bp_element"},{"category":"method","location":"internal_reference/#ByteFloats._check_tabulable-Tuple{ProjSpec}","page":"Internal Reference","text":"Stochastic ρ is a distribution over R, never a table (design §5.4).\n\n\n\n\n\n","title":"ByteFloats._check_tabulable"},{"category":"method","location":"internal_reference/#ByteFloats._comparable-Tuple{Binary, Binary}","page":"Internal Reference","text":"Numeric comparison is defined only when neither operand is NaN — every ==/</<= below returns false otherwise, per IEEE unorderedness.\n\n\n\n\n\n","title":"ByteFloats._comparable"},{"category":"method","location":"internal_reference/#ByteFloats._resolve_rng-Tuple{Union{Nothing, Random.AbstractRNG}}","page":"Internal Reference","text":"Resolve a caller-supplied rng, falling back to the task-local default. Call sites hoist this out of loops so array kernels resolve once per call, not per element.\n\n\n\n\n\n","title":"ByteFloats._resolve_rng"},{"category":"method","location":"internal_reference/#ByteFloats._rng_for-Tuple{ProjSpec, Union{Nothing, Random.AbstractRNG}}","page":"Internal Reference","text":"The rng an operation under ρ will actually draw from — nothing for pure ρ, which must never touch RNG state.\n\n\n\n\n\n","title":"ByteFloats._rng_for"},{"category":"method","location":"internal_reference/#ByteFloats._ternary_table_for-Tuple{Symbol, Type{<:Binary}, Type{<:Binary}, Type{<:Binary}, Type{<:Binary}, ProjSpec, Int64}","page":"Internal Reference","text":"_ternary_table_for(op, fr, f1, f2, f3, ρ, nelems) -> Union{Nothing,Memory{UInt8}}\n\nThe kernel-facing policy gate, called once per array operation with the call's element count. Eager band (Σ Kᵢ ≤ TERNARY_EAGER_BITS[]): fetch/build now. Adaptive band (≤ TERNARY_ADAPTIVE_BITS[]): return the cached table if present, otherwise accumulate nelems against the signature and build only once it has earned TERNARY_BUILD_ELEMS[]. Beyond the adaptive band (the K=8 territory): always nothing — the compute kernel is the right tradeoff there.\n\n\n\n\n\n","title":"ByteFloats._ternary_table_for"},{"category":"method","location":"internal_reference/#ByteFloats.apply_op-Union{Tuple{fr}, Tuple{Val, Type{fr}, ProjSpec, Int64, Vararg{Float64}}} where fr<:Binary","page":"Internal Reference","text":"apply_op(Val(op), fr, ρ, R, xs...) — evaluate op's ω-semantics on decoded Float64 operands and project the result into fr under ρ (with random bits R).\n\n\n\n\n\n","title":"ByteFloats.apply_op"},{"category":"method","location":"internal_reference/#ByteFloats.blockdecode-Union{Tuple{Block{B}}, Tuple{B}} where B","page":"Internal Reference","text":"ωBlockDecode (draft §5.1.1): per lane ωMultiply(decode(s), decode(xᵢ)) — exact Float64.\n\n\n\n\n\n","title":"ByteFloats.blockdecode"},{"category":"method","location":"internal_reference/#ByteFloats.blockproject-Union{Tuple{B}, Tuple{Type{<:Binary}, ProjSpec, Binary, NTuple{B, Any}}} where B","page":"Internal Reference","text":"ωBlockProject (draft §5.1.2) over a tuple of result kinds, scale supplied as a value.\n\n\n\n\n\n","title":"ByteFloats.blockproject"},{"category":"method","location":"internal_reference/#ByteFloats.encode-Union{Tuple{EXT}, Tuple{SGN}, Tuple{P}, Tuple{K}, Tuple{Type{Binary{K, P, SGN, EXT}}, Int64, Int64, Int64}} where {K, P, SGN, EXT}","page":"Internal Reference","text":"encode(T, sign, S, Q) -> UInt8   (private; design §3.3)\n\nωEncode from canonical integer form: value = sign · S · 2^Q, with S ∈ 0:2^P (S == 2^P is the next-binade carry, draft §4.7.4 NOTE 4). Precondition: the value is in the datum set of T (guaranteed by RoundToPrecision ∘ Saturate).\n\n\n\n\n\n","title":"ByteFloats.encode"},{"category":"function","location":"internal_reference/#ByteFloats.get_table","page":"Internal Reference","text":"get_table(op, fr, f1, [f2,] ρ) -> Memory{UInt8}\n\nFetch (building and caching on first use) the complete result table for the pure-ρ specialization op⟨fr; f1[, f2]⟩ under ρ: entry c + 1 (unary) or (c1 << K2) + c2 + 1 (binary) holds the result code point for those operand code points. Throws for stochastic ρ. @noinline by design: kernels call this once per array operation and index the returned table in their hot loop.\n\n\n\n\n\n","title":"ByteFloats.get_table"},{"category":"method","location":"internal_reference/#ByteFloats.get_table-Tuple{Symbol, Type{<:Binary}, Type{<:Binary}, Type{<:Binary}, Type{<:Binary}, ProjSpec}","page":"Internal Reference","text":"get_table(op, fr, f1, f2, f3, ρ) -> Memory{UInt8}\n\nTernary form: entry ((c1 << K2 | c2) << K3) + c3 + 1 holds the result code point. Builds unconditionally (policy lives in _ternary_table_for); pure ρ only.\n\n\n\n\n\n","title":"ByteFloats.get_table"},{"category":"method","location":"internal_reference/#ByteFloats.project-Union{Tuple{SM}, Tuple{RM}, Tuple{T}, Tuple{EXT}, Tuple{SGN}, Tuple{P}, Tuple{K}, Tuple{Type{T}, ProjSpec{RM, SM}, Any}} where {K, P, SGN, EXT, T<:Binary{K, P, SGN, EXT}, RM, SM}","page":"Internal Reference","text":"project(T, ρ, X; R=0, sticky=0) -> T\n\nProject a closed-extended-real carrier value into format T under ρ. X::Float64 must be exact (the Float64 fast path); X::BigFloat likewise (or an interval endpoint with sticky ∈ {-1,+1}). R is the stochastic draw.\n\n\n\n\n\n","title":"ByteFloats.project"},{"category":"method","location":"internal_reference/#ByteFloats.project_interval-Union{Tuple{T}, Tuple{Type{T}, ProjSpec, Any}} where T<:Binary","page":"Internal Reference","text":"project_interval(T, ρ, f; R=0, maxprec=4096) -> T\n\nProject the true value of f, where f(prec) returns an enclosure (d::BigFloat, u::BigFloat) with the true value in [d, u] (endpoints from MPFR directed rounding). Rigor (design §9.2): if d == u the value is exact — project it. Otherwise the true value lies in the open interval (d, u) (directed correct rounding of a non-representable value moves strictly); projection is monotone in the value for every mode at fixed R, so if project(d, sticky=+1) == project(u, sticky=-1) that common code is the answer; otherwise a projection grid point sits inside the interval and precision escalates.\n\n\n\n\n\n","title":"ByteFloats.project_interval"},{"category":"method","location":"internal_reference/#ByteFloats.unpack_tile!-Union{Tuple{F}, Tuple{AbstractVector{F}, PackedVector{F}, Int64, Int64}} where F","page":"Internal Reference","text":"Unpack pv[first:first+len-1] into buf (byte scratch); tile-granular kernel entry.\n\n\n\n\n\n","title":"ByteFloats.unpack_tile!"},{"category":"section","location":"user_guide/#User-Guide","page":"User Guide","text":"Everything you need to use ByteFloats.jl, in the order you'll need it. All transcripts in this guide are captured from real sessions against the shipped package.","title":"User Guide"},{"category":"section","location":"user_guide/#Formats-and-format-queries","page":"User Guide","text":"The parametric type Binary{K,P,SGN,EXT} defines a format: bitwidth K ∈ 3:8, precision P (significand bits including the implicit bit), SGN::Bool for signed/unsigned, EXT::Bool for extended (with ±Inf) versus finite. Every legal combination — 120 formats — has its draft name exported:\n\njulia> Binary8p4se === Binary{8,4,true,true}\ntrue\n\njulia> formatname(Binary{6,3,true,false})\n:Binary6p3sf\n\nThe suffix reads: p4 precision 4, s/u signed/unsigned, e/f extended/finite.\n\nFormat introspection follows the draft's Group M, with both Julia-style and draft-named accessors:\n\njulia> bitwidth(Binary8p4se), precision(Binary8p4se), expbias(Binary8p4se)\n(8, 4, 8)\n\njulia> MaxFiniteOf(Binary8p4se)\nBinary8p4se(224.0 ≡ 0x7e)\n\njulia> MinPositiveOf(Binary8p4se)\nBinary8p4se(0.0009765625 ≡ 0x01)\n\nAlso available: MinFiniteOf, MinNormalOf, MaxSubnormalOf, expbitwidth, trailingsigbits, issigned, isextended, and the draft-named forms (BitwidthOf, PrecisionOf, ExponentBiasOf, …).\n\nEach of these accepts a value as well as a type — the answer is a pure function of the format's type parameters, so bitwidth(x) and bitwidth(typeof(x)) are the same query and both fold to a literal:\n\njulia> x = Binary8p4se(1.6);\n\njulia> BitwidthOf(x), PrecisionOf(x), SignednessOf(x), DomainOf(x)\n(8, 4, true, true)\n\njulia> MaxFiniteOf(x)\nBinary8p4se(224.0 ≡ 0x7e)","title":"Formats and format queries"},{"category":"section","location":"user_guide/#Values","page":"User Guide","text":"A value is one byte: an immutable wrapper around its code point. Four ways in, two ways out:\n\njulia> x = Binary8p4se(1.6)              # construct from a Real: projects (rounds)\nBinary8p4se(1.625 ≡ 0x45)\n\njulia> Binary8p4se(0x45)                 # construct from a UInt8 CODE POINT (validated)\nBinary8p4se(1.625 ≡ 0x45)\n\njulia> rawvalue(Binary8p4se, 0x45)       # same, unchecked — the kernel-internal route\nBinary8p4se(1.625 ≡ 0x45)\n\njulia> Convert(Binary8p4se, RNE_SatNone, 3)   # explicit conversion, any mode\nBinary8p4se(3.0 ≡ 0x4c)\n\njulia> decode(x)                          # exact datum as Float64 (always exact)\n1.625\n\njulia> codepoint(x)                       # the raw byte (extends Base.codepoint)\n0x45\n\nnote: UInt8 means code point; every other number means value\nUInt8 is the only argument type with code-point semantics — mirroring Char(0x41). Every other Real (including other Integers) constructs by projecting the numeric value, and Convert is numeric for all integers:julia> Binary8p4se(0x02), Binary8p4se(2)\n(Binary8p4se(0.001953125 ≡ 0x02), Binary8p4se(2.0 ≡ 0x48))Out-of-range codes throw for K < 8 (Binary5p3sf(0x20) is an error); the range check costs nothing measurable — 2.1 ns, identical to unchecked rawvalue. Round-tripping is T(codepoint(x)) === x.\n\nConvert accepts Binary values (any format), Float16/32/64, Float128, Integer, and BigFloat — types whose values it can project exactly. Float128 projects directly, preserving all 113 significand bits: a value that sits a hair above a rounding midpoint converts correctly where staging through Float64 would round it onto the midpoint first and then break the tie the wrong way. For other Reals, convert explicitly first (e.g. Binary8p4se(Float64(π))) and own the double rounding; Rational inputs throw with that guidance rather than double-round silently.\n\nEvery format has exactly one NaN and no negative zero; extended formats add ±Inf:\n\njulia> Binary8p4se(1e9)                   # overflow under the default spec → +Inf\nBinary8p4se(Inf ≡ 0x7f)\n\njulia> Binary8p4se(-0.0)                  # no −0: projects to the single zero\nBinary8p4se(0.0 ≡ 0x00)\n\njulia> isnan(Binary8p4se(NaN)), isfinite(Binary8p4se(2.0))\n(true, true)\n\nStandard predicates (isnan, isinf, isfinite, iszero, signbit, issubnormal), zero/one/eps/typemin/typemax, and stepping via NextGreaterThan / NextLessThan (the draft's Next operations; nextfloat/prevfloat alias them) all work:\n\njulia> NextGreaterThan(Binary8p4se(1.0))\nBinary8p4se(1.125 ≡ 0x41)\n\njulia> Class(Binary8p4se(0.01))           # draft classification\nClassPosNormal::FPClass = 0x06","title":"Values"},{"category":"section","location":"user_guide/#Random-values","page":"User Guide","text":"The Random API works on every format, in every setup Julia programmers expect.\n\nWhat the draws mean. rand draws the real uniform on [0, 1) at Float64 and floor-projects it onto the format's grid, so each code point receives exactly the real measure of its interval; results are always in [0, 1). randn projects a standard-normal Float64 draw round-to-nearest with SatFinite, so tail draws beyond MaxFiniteOf(T) clamp to the extremal finite datum — randn never returns ±Inf or NaN, which matters for tiny-range formats (Binary3p1se has MaxFinite = 1.0). randn requires a signed format; an unsigned format throws:\n\njulia> randn(Binary6p3ue)\nERROR: ArgumentError: randn requires a signed format; Binary6p3ue cannot represent negative draws\n\nSetup 1 — quick and implicit. No rng argument: draws come from Julia's task-local default generator (a Xoshiro), and Random.seed! controls them exactly as for any other type:\n\njulia> Random.seed!(1234); rand(Binary8p4se)\nBinary8p4se(0.3125 ≡ 0x32)\n\njulia> Random.seed!(1234); rand(Binary8p4se)      # same seed, same draw\nBinary8p4se(0.3125 ≡ 0x32)\n\nSetup 2 — an explicit rng stream. Pass any AbstractRNG first, for reproducible streams independent of global state:\n\njulia> rand(Xoshiro(1), Binary8p4se)\nBinary8p4se(0.0703125 ≡ 0x21)\n\njulia> rand(Xoshiro(8), Binary8p4se, 4)\n4-element Vector{Binary8p4se}:\n   Binary8p4se(0.40625 ≡ 0x35)\n Binary8p4se(0.021484375 ≡ 0x13)\n      Binary8p4se(0.75 ≡ 0x3c)\n   Binary8p4se(0.28125 ≡ 0x31)\n\nSetup 3 — arrays and in-place. rand(T, dims...) / randn(T, dims...) build Array{T}; rand!(A) / randn!(A) fill an existing array (one byte per element — cheap to preallocate and reuse):\n\njulia> A = Vector{Binary8p4se}(undef, 3); randn!(Xoshiro(2), A)\n3-element Vector{Binary8p4se}:\n Binary8p4se(-0.005859375 ≡ 0x86)\n        Binary8p4se(1.75 ≡ 0x46)\n        Binary8p4se(-1.0 ≡ 0xc0)\n\njulia> randn(Xoshiro(3), Binary8p4se, 2, 3) |> typeof\nMatrix{Binary8p4se}\n\nFeed array draws straight into the storage layers when that is the goal: PackedVector(rand(Binary4p2se, n)).\n\nSetup 4 — choosing the projection. The scalar ::Type forms take a projection keyword to land the draw under any ProjSpec:\n\njulia> rand(Xoshiro(2), Binary8p4se; projection = RTP_SatNone)    # ceiling\nBinary8p4se(0.0029296875 ≡ 0x03)\n\njulia> randn(Xoshiro(6), Binary8p4se; projection = RTZ_SatFinite) # toward zero\nBinary8p4se(-1.875 ≡ 0xc7)\n\njulia> rand(Xoshiro(4), Binary8p4se; projection = RSA_SatNone(8)) # stochastic\nBinary8p4se(0.8125 ≡ 0x3d)\n\nA stochastic projection draws its random bits from the same rng, so seeded streams stay reproducible. The defaults are the contract-keepers (floor for rand, nearest + SatFinite for randn); opting out can produce 1.0 from rand or ±Inf/NaN from randn. The array and ! forms always use the defaults — for arrays under another projection, draw scalars: [rand(rng, T; projection = ρ) for _ in 1:n]. Worked transcripts, including the K = 3 tail-clamp comparison: User Examples.","title":"Random values"},{"category":"section","location":"user_guide/#Projection-specifications","page":"User Guide","text":"Every rounding decision in the package is governed by a ProjSpec, the pair of a rounding mode and a saturation mode. Both are zero-size singleton types, so a ProjSpec costs nothing and fully specializes every call it reaches.\n\nRounding modes: NearestTiesToEven, NearestTiesToAway, TowardPositive, TowardNegative, TowardZero, ToOdd, and the stochastic families StochasticA{N}, StochasticB{N}, StochasticC{N} with a random-bit budget 1 ≤ N ≤ 60. Saturation modes: SatFinite, SatPropagate, SatNone.\n\njulia> ρ = ProjSpec(TowardPositive(), SatFinite())\n(TowardPositive, SatFinite)\n\njulia> RTP_SatFinite === ρ                # every pairing is also predefined\ntrue","title":"Projection specifications"},{"category":"section","location":"user_guide/#Predefined-specs","page":"User Guide","text":"Every deterministic (rounding, saturation) pairing is exported as a constant, named R<mode>_Sat<mode>:\n\n SatFinite SatPropagate SatNone\nNearestTiesToEven RNE_SatFinite RNE_SatPropagate RNE_SatNone\nNearestTiesToAway RNA_SatFinite RNA_SatPropagate RNA_SatNone\nTowardPositive RTP_SatFinite RTP_SatPropagate RTP_SatNone\nTowardNegative RTN_SatFinite RTN_SatPropagate RTN_SatNone\nTowardZero RTZ_SatFinite RTZ_SatPropagate RTZ_SatNone\nToOdd RTO_SatFinite RTO_SatPropagate RTO_SatNone\n\nThe stochastic families are parameterized by the random-bit budget N, so their predefined forms are constructors rather than constants: RSA_* for StochasticA, RSB_* for StochasticB, RSC_* for StochasticC, each crossed with the three saturation modes. Call them with the budget — or without, for the default N = 8 (ByteFloats.DEFAULT_RBITS):\n\njulia> RSA_SatNone()                      # StochasticA with the default N = 8\n(StochasticA[8], SatNone)\n\njulia> RSC_SatFinite(16) === ProjSpec(StochasticC{16}(), SatFinite())\ntrue\n\nRNE_SatNone is the package's initial default spec. default_projspec reads the session default (see Session defaults below), so every Base-register operator, the same-format convenience methods, and T(x::Real) construction follow DefaultProjection() — RNE_SatNone until you change it.\n\nRounding chooses the neighbor; saturation decides what out-of-range results become. Watch all three interact on an overflowing product in Binary8p4se (MaxFinite = 224):\n\njulia> w, two = Binary8p4se(200.0), Binary8p4se(2.0);\n\njulia> Multiply(Binary8p4se, RNE_SatNone, w, two)       # overflow → ±Inf\nBinary8p4se(Inf ≡ 0x7f)\n\njulia> Multiply(Binary8p4se, RNE_SatFinite, w, two)     # overflow → MaxFinite\nBinary8p4se(224.0 ≡ 0x7e)\n\njulia> Multiply(Binary8p4se, RTZ_SatNone, w, two)\nBinary8p4se(224.0 ≡ 0x7e)   # SatNone + directed-toward-zero clamps finite\n\nnote: Note\nSatNone is not \"no saturation handling\": it is the draft's row set in which nearest modes overflow to ±Inf (or NaN for finite formats) while directed modes that point away from the overflow clamp to the extremal finite value.","title":"Predefined specs"},{"category":"section","location":"user_guide/#Stochastic-rounding","page":"User Guide","text":"Stochastic modes consume N random bits per projection. You control the source three ways — implicitly (task-local RNG), by passing an rng, or by fixing the draw R itself, which makes single projections exactly reproducible and is the right tool for tests:\n\njulia> σ = RSA_SatNone();                 # ≡ ProjSpec(StochasticA{8}(), SatNone())\n\njulia> Add(Binary8p4se, σ, Binary8p4se(2.0), Binary8p4se(0.03125); rng = Xoshiro(1))\nBinary8p4se(2.0 ≡ 0x48)\n\njulia> Add(Binary8p4se, σ, Binary8p4se(2.0), Binary8p4se(0.03125); R = 0)\nBinary8p4se(2.0 ≡ 0x48)      # smallest draw: rounds down\n\njulia> Add(Binary8p4se, σ, Binary8p4se(2.0), Binary8p4se(0.03125); R = 255)\nBinary8p4se(2.25 ≡ 0x49)     # largest draw: rounds up\n\nThe exact fraction here is 1/8 of an ulp, so over all 256 draws exactly 32 round up — StochasticA is unbiased in expectation. R must lie in 0:2^N-1.","title":"Stochastic rounding"},{"category":"section","location":"user_guide/#Session-defaults","page":"User Guide","text":"Six session-wide defaults are readable as DefaultX() and settable as DefaultX!(v):\n\ndefault initial value setter accepts\nDefaultType Binary8p2se any fully-parameterized Binary type\nDefaultReturnType Binary8p2se any fully-parameterized Binary type\nDefaultAccumulatorType binary32 (Float32) any AbstractFloat type\nDefaultRoundingMode NearestTiesToEven() mode instance or type\nDefaultSaturationMode SatNone() mode instance or type\nDefaultProjection RNE_SatNone a ProjSpec, or (mode, sat)\nDefaultRNG the Xoshiro type RNG type or instance\nDefaultRbits 8 Int in 1:60\n\nThe projection default and its components are kept coherent in both directions: setting DefaultRoundingMode! or DefaultSaturationMode! rebuilds DefaultProjection around the change, and setting DefaultProjection! directly decomposes it back into both components — so DefaultProjection() === ProjSpec(DefaultRoundingMode(), DefaultSaturationMode()) always holds.\n\njulia> DefaultRoundingMode!(TowardZero())\nTowardZero()\n\njulia> DefaultProjection()               # followed the component\n(TowardZero, SatNone)\n\njulia> DefaultProjection!(RNA_SatFinite)\n(NearestTiesToAway, SatFinite)\n\njulia> DefaultRoundingMode(), DefaultSaturationMode()   # followed the projection\n(NearestTiesToAway(), SatFinite())\n\nDefaultProjection is not merely advisory: default_projspec reads it, so the same-format convenience methods (a + b, Exp(x), …), the Base-register operators, and T(x::Real) construction all follow it.\n\njulia> x, y = Binary8p4se(200.0), Binary8p4se(2.0);\n\njulia> x * y                              # RNE_SatNone: overflow → +Inf\nBinary8p4se(Inf ≡ 0x7f)\n\njulia> DefaultProjection!(RTZ_SatFinite);\n\njulia> x * y                              # now clamps to MaxFinite\nBinary8p4se(224.0 ≡ 0x7e)\n\njulia> Multiply(Binary8p4se, RNE_SatNone, x, y)   # explicit ρ is unaffected\nBinary8p4se(Inf ≡ 0x7f)\n\nwarning: Changing the default is a global semantic change\nEvery caller of the convenience forms — including code in other packages — sees it. Library code that needs a specific projection should name it explicitly rather than rely on the session default.\n\nFollowing the default costs nothing while it holds its initial value: the convenience forms consume it through the same speculation guard as the with_default_* combinators, so they compile against the constant and stay allocation-free with concretely inferred results (pinned in the test suite). Once you change the default they cross a function barrier instead — one dynamic dispatch per call, everything inside still specialized. The explicit forms Add(T, ρ, x, y) are unaffected either way.\n\nTo consume a default in your own code without paying dynamic-dispatch costs, go through the with_default_* combinators — with_default_type, with_default_returntype, with_default_accumulatortype, with_default_projection — which call f(default, args...):\n\njulia> with_default_type((T, x) -> T(x), 1.5)\nBinary8p2se(1.5 ≡ 0x41)\n\nWhile a default still holds its initial value, the combinator's call is statically compiled against that constant — no dynamic dispatch. After the default is changed it crosses a function barrier: one dynamic dispatch at entry, everything inside fully specialized.\n\nAllocation contract: when f's result type does not depend on the default — with_default_projection with the formats fixed by the caller is the normal shape — the call is zero-allocation with a concretely inferred result (pinned in the test suite). When the result's type is the default (with_default_type used as a constructor), the value is computed on the specialized path but boxes once where it escapes — the irreducible cost of a runtime-chosen type.","title":"Session defaults"},{"category":"section","location":"user_guide/#Scalar-operations:-the-two-registers","page":"User Guide","text":"The spec-named register exposes every draft operation under its draft name with an explicit result format and spec: Op(fr, ρ, operands...). Operands may be any formats; the result format is the first argument. The catalog:\n\n30 unary — Abs, Negate, Sqrt, RSqrt, Recip, Exp, Exp2, ExpMinusOne, Log, Log2, LogOnePlus, Softplus, the trig/hyperbolic families, and the π-scaled families;\n18 binary — Add, Subtract, Multiply, Divide, CopySign, Hypot, ArcTan2, ArcTan2Pi, and the eight Minimum/Maximum variants plus the Finite variants;\n3 ternary — FMA, FAA, Clamp;\nConvert — the one operation that also accepts non-Binary operands.\n\nThe Base register makes each format an ordinary Julia number under its default spec (RNE_SatNone): + - * /, fma, exp, log, sqrt, min, max, abs, atan(y, x), sinpi, inv, comparisons, and friends — same-format operands only, no silent cross-format promotion (mixing formats promotes to Float64 explicitly).\n\njulia> exp(Binary8p4se(0.25))                       # Base register\nBinary8p4se(1.25 ≡ 0x42)\n\njulia> Exp(Binary8p4se, RNE_SatNone, Binary8p4se(0.25))   # identical, explicit\nBinary8p4se(1.25 ≡ 0x42)\n\nSemantics highlights (all per the draft): x/0 → NaN for every x including ±Inf; Recip(±Inf) = 0; 0·∞ → NaN; a NaN operand generally propagates, except the *Number/*Finite extremum variants which prefer non-NaN (and finite) operands; trig of ±Inf is NaN; the π-scaled family reduces exactly mod 2 first.","title":"Scalar operations: the two registers"},{"category":"section","location":"user_guide/#Arrays,-kernels,-and-sorting","page":"User Guide","text":"Every operation has array methods — Op(fr, ρ, A), Op(fr, ρ, A, B), Op(fr, ρ, A, B, C) — plus the generic vmap / vmap!:\n\njulia> A = Binary8p4se.(randn(Xoshiro(7), 4) .* 2)\n4-element Vector{Binary8p4se}:\n Binary8p4se(-0.875 ≡ 0xbe)\n Binary8p4se(4.0 ≡ 0x50)\n Binary8p4se(-3.25 ≡ 0xcd)\n Binary8p4se(2.25 ≡ 0x49)\n\njulia> Exp(Binary8p4se, RNE_SatNone, A)\n4-element Vector{Binary8p4se}:\n Binary8p4se(0.40625 ≡ 0x35)\n Binary8p4se(56.0 ≡ 0x6e)\n Binary8p4se(0.0390625 ≡ 0x1a)\n Binary8p4se(9.0 ≡ 0x59)\n\nFor pure (non-stochastic) specs, unary and binary array calls run as table gathers: the first call builds and caches a 256-byte (unary) or 64 KiB (binary) result table for that exact (op, formats, ρ) specialization, and every later element costs a single lookup — measured at 0.27 ns/element unary, 0.5 ns/element binary.\n\nTernary operations (FMA, FAA, Clamp) ride the same gather whenever the three operand bitwidths keep the table affordable, tiered by K1 + K2 + K3:\n\nEager (up to 256 KiB; every all-K ≤ 6 signature): built on the first array call, like unary/binary.\nAdaptive (up to 2 MiB; the K = 7 band): built only once a signature has processed enough elements to earn its build, in a byte-bounded, LRU-evicted cache.\nCompute (K = 8; a 16 MiB table stops being a cache win): the scalar pipeline runs per element, optionally threaded for long arrays.\n\nEvery table entry — eager or adaptive — is built through the scalar path, so it is bit-identical by construction. Stochastic calls always run the scalar pipeline per element (each element draws its own random bits).\n\nInspect or reset the cache — unary/binary and ternary alike — with table_bytes() and empty_tables!(). The ternary policy thresholds and the threading cutoff are internal Refs (ByteFloats.TERNARY_EAGER_BITS and neighbors in tables.jl and kernels.jl) for the rare case you need to tune them.\n\nSorting is special-cased: values compare through integer order keys, and vectors of Binary sort with an O(n) counting sort installed as the default algorithm — about 8× the stock comparison sort at 64 K elements, rev=true included. sort(A) just works; NaN sorts last (first under rev=true), matching Base's conventions. TotalOrder(x, y) exposes the draft's total order directly (single NaN largest).","title":"Arrays, kernels, and sorting"},{"category":"section","location":"user_guide/#Blocks-and-scaled-operations","page":"User Guide","text":"A Block{B,FS,FE} is a scale in format FS and B elements in format FE; its represented values are scale × element (the MX-style scheme). Construct directly or quantize a tuple with the draft's algorithm, which picks the scale from the maximum finite |element| and projects each element against it:\n\njulia> b = Block(Binary8p1uf(4.0), Binary8p4se(1.5), Binary8p4se(-0.75),\n                 Binary8p4se(2.0), Binary8p4se(0.5))\nBlock{4, Binary8p1uf, Binary8p4se}(Binary8p1uf(4.0 ≡ 0x82), (…))\n\njulia> ConvertFromBlock(Binary8p3se, RNE_SatNone, b)   # decode scale×elem, project\n(Binary8p3se(6.0 ≡ 0x4a), Binary8p3se(-3.0 ≡ 0xc6), Binary8p3se(8.0 ≡ 0x4c), Binary8p3se(2.0 ≡ 0x44))\n\njulia> BlockReduceAdd(Binary8p4se, RNE_SatNone, b)     # exact Σ scale·xᵢ, then project\nBinary8p4se(13.0 ≡ 0x5d)\n\njulia> ConvertToBlockMaxAbsFinite(Binary8p1uf, Binary8p4se, RNE_SatNone, RNE_SatNone,\n           (Binary8p4se(100.0), Binary8p4se(-12.0), Binary8p4se(0.5), Binary8p4se(3.0)))\nBlock{4, Binary8p1uf, Binary8p4se}(Binary8p1uf(64.0 ≡ 0x86), (…))\n\nEvery scalar operation lifts to blocks (BlockAdd, BlockExp, …, taking blocks and a result scale) and to the scaled form ScaledOp (ScaledAdd(fr, ρ, s1, x1, s2, x2) — block size 1). Reductions: BlockReduceAdd, BlockReduceMultiply, and BlockDotProduct(fr, ρ, bx, by), whose lane products and accumulation are exact before the single final projection — there is no hidden intermediate rounding. ConvertToBlock(fs, fr, ρ, xs, s) quantizes against a scale you supply. BlockVector stores many same-shape blocks in structure-of-arrays layout.","title":"Blocks and scaled operations"},{"category":"section","location":"user_guide/#Packed-storage","page":"User Guide","text":"For memory-bound work, PackedVector{F} stores code points at bitwidth(F) bits per element (a Binary5p2se vector at 5 bits/element instead of 8):\n\njulia> v = Binary5p2se.(rand(Xoshiro(3), 6) .* 4);\n\njulia> pv = PackedVector(v); sizeof(pv.data)\n8                     # 6 × 5 bits = 30 bits → one 64-bit word\n\njulia> pv[3] == v[3]\ntrue\n\nIt is a full AbstractVector{F} (indexing, collect, iteration), and vmap accepts it directly, unpacking cache-friendly tiles internally. The rule is store packed, compute unpacked: there is deliberately no in-place packed arithmetic.","title":"Packed storage"},{"category":"section","location":"user_guide/#Conformance-and-κ-approximate-implementations","page":"User Guide","text":"conformance() returns the live declaration — formats, operations, mode vocabulary, the table specializations actually instantiated this session, and every registered approximation. conformance_report() prints it; conformance_dict() returns a plain nested Dict for JSON/TOML serialization.\n\nThe default API is bit-exact, always. If you want a faster inexact kernel, register it — and the registry measures your honesty:\n\njulia> ftz = ftz_variant(:Exp, Binary8p4se, Binary8p4se, RNE_SatFinite);  # Annex example\n\njulia> impl = register_approx!(:my_fast_exp, :Exp, Binary8p4se, (Binary8p4se,),\n                               RNE_SatFinite, ftz);\n\njulia> kappa(:my_fast_exp)      # measured max code-point deviation, verified exhaustively\n4.0\n\nDeclaring κ smaller than the measured deviation throws; NaN-mismatching implementations must be acknowledged with an explicit κ = NaN. Retrieve with approx(:my_fast_exp), list with list_approx(), and measure anything yourself with measure_kappa / codedistance.","title":"Conformance and κ-approximate implementations"},{"category":"section","location":"user_guide/#Performance-guidance","page":"User Guide","text":"Pass format types statically. Through const bindings, type parameters, or function arguments, every entry point fully specializes (scalar Add ≈ 18–26 ns, project ≈ 13 ns, zero allocations). A format type read from a non-const global forces Julia's dynamic dispatch on every call (~1 µs for keyword calls); one function barrier f(::Type{T}, …) where {T} restores full speed.\nThe convenience forms are free at the initial default. x + y, Exp(x), and T(2.1) read the session default through a speculation guard, so they are allocation-free and concretely inferred while it holds its initial value. After you change the default they cost one dynamic dispatch per call; name ρ explicitly (Add(T, ρ, x, y) with a const ρ) in hot code that must be insensitive to the session default.\nBulk work belongs in array calls. The table-gather kernels are ~50× the scalar path; the first call per specialization pays a one-time build (≈ 0.4 ms unary, tens of ms for 8×8 binary tables, up to a few ms for a 2 MiB ternary table).\nTernary array calls scale with bitwidth. K ≤ 6 operand formats table eagerly (~35× the scalar loop); K = 7 tables adaptively once a signature is hot; K = 8 runs the compute kernel, optionally threaded for long arrays (~4× at 4 threads) — no action needed, the array call picks the right path.\nStochastic array calls run the scalar pipeline per element; pass an explicit rng for reproducibility.\nMemory: PackedVector for storage; BlockVector for many blocks.\nThe reproducible benchmark suite lives at benchmarking/benchmarking.jl and generates a full markdown report for your machine.","title":"Performance guidance"},{"category":"section","location":"user_guide/#Environment-switches","page":"User Guide","text":"ENV[\"ByteFloats_Float128\"] = \"disable\" (set before using ByteFloats) disables the internal Float128 fast paths in favor of pure MPFR — results are bit-identical (tested); only build/oracle speed changes.","title":"Environment switches"},{"category":"section","location":"external_reference/#External-Reference","page":"External Reference","text":"Docstrings for the documented public surface — the exported names. The full export list is far larger (400+ names, including the 120 format aliases, the predefined projection-spec grid, and the generated operation and block registers); names without docstrings are covered descriptively in the User Guide; unexported machinery is in the Internal Reference.","title":"External Reference"},{"category":"module","location":"external_reference/#ByteFloats.ByteFloats","page":"External Reference","text":"ByteFloats\n\nA conforming, performance-oriented Julia implementation of the IEEE P3109 draft standard for arithmetic formats for machine learning (bitwidths 3–8).\n\nBit-exact defined results on every default path; the projection engine (RoundToPrecision → Saturate → Encode) is the single write path into a code point; approximate fast paths exist only behind the explicit κ registry (register_approx! / approx), never substituted silently. See conformance() for the live declaration and ByteFloats.draft_revision() for the implemented draft.\n\nPerformance note\n\nPass format types through const bindings, type parameters, or function arguments. All exported entry points fully specialize when the format type is statically known (measured: a complete scalar Add ≈ 26 ns, project ≈ 13 ns, zero allocations). Calling through a non-const global format type forces dynamic dispatch on every call — measured at ~1 µs per scalar keyword call — which is Julia semantics, not package overhead; a single function barrier (f(::Type{T}, args...) where {T}) restores full speed. The test suite pins the specialization properties (concrete return types, zero warm-path allocation) as deterministic regressions.\n\n\n\n\n\n","title":"ByteFloats.ByteFloats"},{"category":"constant","location":"external_reference/#ByteFloats.RNA_SatFinite","page":"External Reference","text":"(NearestTiesToAway, SatFinite).\n\n\n\n\n\n","title":"ByteFloats.RNA_SatFinite"},{"category":"constant","location":"external_reference/#ByteFloats.RNA_SatNone","page":"External Reference","text":"(NearestTiesToAway, SatNone).\n\n\n\n\n\n","title":"ByteFloats.RNA_SatNone"},{"category":"constant","location":"external_reference/#ByteFloats.RNA_SatPropagate","page":"External Reference","text":"(NearestTiesToAway, SatPropagate).\n\n\n\n\n\n","title":"ByteFloats.RNA_SatPropagate"},{"category":"constant","location":"external_reference/#ByteFloats.RNE_SatFinite","page":"External Reference","text":"(NearestTiesToEven, SatFinite).\n\n\n\n\n\n","title":"ByteFloats.RNE_SatFinite"},{"category":"constant","location":"external_reference/#ByteFloats.RNE_SatNone","page":"External Reference","text":"(NearestTiesToEven, SatNone) — the package-wide default ρ.\n\n\n\n\n\n","title":"ByteFloats.RNE_SatNone"},{"category":"constant","location":"external_reference/#ByteFloats.RNE_SatPropagate","page":"External Reference","text":"(NearestTiesToEven, SatPropagate).\n\n\n\n\n\n","title":"ByteFloats.RNE_SatPropagate"},{"category":"constant","location":"external_reference/#ByteFloats.RTN_SatFinite","page":"External Reference","text":"(TowardNegative, SatFinite).\n\n\n\n\n\n","title":"ByteFloats.RTN_SatFinite"},{"category":"constant","location":"external_reference/#ByteFloats.RTN_SatNone","page":"External Reference","text":"(TowardNegative, SatNone).\n\n\n\n\n\n","title":"ByteFloats.RTN_SatNone"},{"category":"constant","location":"external_reference/#ByteFloats.RTN_SatPropagate","page":"External Reference","text":"(TowardNegative, SatPropagate).\n\n\n\n\n\n","title":"ByteFloats.RTN_SatPropagate"},{"category":"constant","location":"external_reference/#ByteFloats.RTO_SatFinite","page":"External Reference","text":"(ToOdd, SatFinite).\n\n\n\n\n\n","title":"ByteFloats.RTO_SatFinite"},{"category":"constant","location":"external_reference/#ByteFloats.RTO_SatNone","page":"External Reference","text":"(ToOdd, SatNone).\n\n\n\n\n\n","title":"ByteFloats.RTO_SatNone"},{"category":"constant","location":"external_reference/#ByteFloats.RTO_SatPropagate","page":"External Reference","text":"(ToOdd, SatPropagate).\n\n\n\n\n\n","title":"ByteFloats.RTO_SatPropagate"},{"category":"constant","location":"external_reference/#ByteFloats.RTP_SatFinite","page":"External Reference","text":"(TowardPositive, SatFinite).\n\n\n\n\n\n","title":"ByteFloats.RTP_SatFinite"},{"category":"constant","location":"external_reference/#ByteFloats.RTP_SatNone","page":"External Reference","text":"(TowardPositive, SatNone).\n\n\n\n\n\n","title":"ByteFloats.RTP_SatNone"},{"category":"constant","location":"external_reference/#ByteFloats.RTP_SatPropagate","page":"External Reference","text":"(TowardPositive, SatPropagate).\n\n\n\n\n\n","title":"ByteFloats.RTP_SatPropagate"},{"category":"constant","location":"external_reference/#ByteFloats.RTZ_SatFinite","page":"External Reference","text":"(TowardZero, SatFinite).\n\n\n\n\n\n","title":"ByteFloats.RTZ_SatFinite"},{"category":"constant","location":"external_reference/#ByteFloats.RTZ_SatNone","page":"External Reference","text":"(TowardZero, SatNone).\n\n\n\n\n\n","title":"ByteFloats.RTZ_SatNone"},{"category":"constant","location":"external_reference/#ByteFloats.RTZ_SatPropagate","page":"External Reference","text":"(TowardZero, SatPropagate).\n\n\n\n\n\n","title":"ByteFloats.RTZ_SatPropagate"},{"category":"type","location":"external_reference/#ByteFloats.ApproxImpl","page":"External Reference","text":"A registered κ-approximate implementation of one operation specialization.\n\n\n\n\n\n","title":"ByteFloats.ApproxImpl"},{"category":"type","location":"external_reference/#ByteFloats.Binary","page":"External Reference","text":"Binary{K,P,SGN,EXT} <: AbstractFloat\n\nA P3109 floating-point value: a code point in the format with bitwidth K ∈ 3:8, precision P, signedness SGN::Bool (true = Signed), domain EXT::Bool (true = Extended, i.e. the datum set includes infinities).\n\nThe code point occupies the low K bits of the payload byte; the high 8-K bits are maintained zero as a representation invariant. Construct raw code points with T(c::UInt8) (validated code-point construction), rawvalue(T, c) (unchecked kernel route), or Binary{...}(Val(:code), x::UInt8); construct from numeric values with T(x::Real) (default projection spec) or Convert. UInt8 is the one argument type meaning code point; all other Reals mean value.\n\n\n\n\n\n","title":"ByteFloats.Binary"},{"category":"type","location":"external_reference/#ByteFloats.Block","page":"External Reference","text":"Block{B, FS<:Binary, FE<:Binary}\n\nA P3109 block (s, [x₁ … x_B]): scale factor in format FS, B ≥ 1 elements in format FE (draft §5). isbits; blocks live in registers/stack.\n\n\n\n\n\n","title":"ByteFloats.Block"},{"category":"type","location":"external_reference/#ByteFloats.BlockVector","page":"External Reference","text":"BlockVector{B,FS,FE} <: AbstractVector{Block{B,FS,FE}}\n\nStructure-of-arrays storage: scales::Vector{FS}, elems::Matrix{FE} (B × n, column-major so each block's elements are one contiguous column).\n\n\n\n\n\n","title":"ByteFloats.BlockVector"},{"category":"type","location":"external_reference/#ByteFloats.FPClass","page":"External Reference","text":"Eight-way datum classification returned by Class (draft §4.13.1).\n\n\n\n\n\n","title":"ByteFloats.FPClass"},{"category":"type","location":"external_reference/#ByteFloats.NearestTiesToAway","page":"External Reference","text":"Round to nearest; ties away from zero (draft §4.7.1).\n\n\n\n\n\n","title":"ByteFloats.NearestTiesToAway"},{"category":"type","location":"external_reference/#ByteFloats.NearestTiesToEven","page":"External Reference","text":"Round to nearest; ties to the even code point (IEEE default; draft §4.7.1).\n\n\n\n\n\n","title":"ByteFloats.NearestTiesToEven"},{"category":"type","location":"external_reference/#ByteFloats.PackedVector","page":"External Reference","text":"PackedVector{F<:Binary} <: AbstractVector{F}\n\nBit-packed storage of F code points at bitwidth(F) bits per element. Construct with PackedVector(A::AbstractVector{F}); recover bytes with Vector(pv) or collect(pv). Memory: ⌈n·K/64⌉ words vs n bytes unpacked.\n\n\n\n\n\n","title":"ByteFloats.PackedVector"},{"category":"type","location":"external_reference/#ByteFloats.ProjSpec","page":"External Reference","text":"ProjSpec{R<:RoundingMode3109, S<:SaturationMode}\n\nA projection specification ρ = (rounding mode, saturation mode), draft §4.2. Zero-size; construct as ProjSpec(NearestTiesToEven(), SatNone()) or via the exported constants. Kernels specialize on its type.\n\n\n\n\n\n","title":"ByteFloats.ProjSpec"},{"category":"type","location":"external_reference/#ByteFloats.StochasticA","page":"External Reference","text":"Stochastic rounding, variant A of draft §4.7.4: RoundAway ⟺ ⌊ν·2^N⌋ + R ≥ 2^N.\n\n\n\n\n\n","title":"ByteFloats.StochasticA"},{"category":"type","location":"external_reference/#ByteFloats.StochasticB","page":"External Reference","text":"Stochastic rounding, variant B: RoundAway ⟺ ⌊ν·2^(N+1)⌋ + (2R+1) ≥ 2^(N+1).\n\n\n\n\n\n","title":"ByteFloats.StochasticB"},{"category":"type","location":"external_reference/#ByteFloats.StochasticC","page":"External Reference","text":"Stochastic rounding, variant C: RoundAway ⟺ RNITE(ν·2^N) + R ≥ 2^N.\n\n\n\n\n\n","title":"ByteFloats.StochasticC"},{"category":"type","location":"external_reference/#ByteFloats.ToOdd","page":"External Reference","text":"Round inexact results to the nearest odd code point (draft §4.7.3; sticky-friendly).\n\n\n\n\n\n","title":"ByteFloats.ToOdd"},{"category":"type","location":"external_reference/#ByteFloats.TowardNegative","page":"External Reference","text":"Directed rounding toward −∞ (draft §4.7.2).\n\n\n\n\n\n","title":"ByteFloats.TowardNegative"},{"category":"type","location":"external_reference/#ByteFloats.TowardPositive","page":"External Reference","text":"Directed rounding toward +∞ (draft §4.7.2).\n\n\n\n\n\n","title":"ByteFloats.TowardPositive"},{"category":"type","location":"external_reference/#ByteFloats.TowardZero","page":"External Reference","text":"Directed rounding toward zero — truncation (draft §4.7.2).\n\n\n\n\n\n","title":"ByteFloats.TowardZero"},{"category":"method","location":"external_reference/#ByteFloats.BlockDotProduct-Union{Tuple{B}, Tuple{Type{<:Binary}, ProjSpec, Block{B}, Block{B}}} where B","page":"External Reference","text":"BlockDotProduct (draft §5.3.2). Lane products can carry 64 significant bits, so the products and their sum are formed in the exact accumulator, with the ∞/NaN fold algebra resolved on the Float64 classifications first.\n\n\n\n\n\n","title":"ByteFloats.BlockDotProduct"},{"category":"method","location":"external_reference/#ByteFloats.BlockReduceAdd-Tuple{Type{<:Binary}, ProjSpec, Block}","page":"External Reference","text":"BlockReduceAdd (draft §5.3.1): project(reduce(ωAdd, [0, X…])).\n\n\n\n\n\n","title":"ByteFloats.BlockReduceAdd"},{"category":"method","location":"external_reference/#ByteFloats.BlockReduceMultiply-Union{Tuple{B}, Tuple{Type{<:Binary}, ProjSpec, Block{B}}} where B","page":"External Reference","text":"BlockReduceMultiply (draft §5.3.1): project(reduce(ωMultiply, [1, X…])).\n\n\n\n\n\n","title":"ByteFloats.BlockReduceMultiply"},{"category":"method","location":"external_reference/#ByteFloats.Class-Tuple{Binary}","page":"External Reference","text":"Class(v) -> FPClass (draft §4.13.1): classify v as NaN, ±Inf, ±normal, ±subnormal, or zero.\n\n\n\n\n\n","title":"ByteFloats.Class"},{"category":"method","location":"external_reference/#ByteFloats.Convert-Tuple{Type{<:Binary}, ProjSpec, Binary}","page":"External Reference","text":"Convert(fr, ρ, x) -> fr\n\nDraft §4.9 Convert⟨fx, fr, ρ⟩. Accepts Binary, IEEE binary16/32/64 floats (widened exactly to the Float64 carrier), Float128 (projected directly), Integer (exact via a sufficiently wide BigFloat), and BigFloat (projected directly; the caller warrants the value is exact).\n\n\n\n\n\n","title":"ByteFloats.Convert"},{"category":"method","location":"external_reference/#ByteFloats.ConvertFromBlock-Union{Tuple{B}, Tuple{Type{<:Binary}, ProjSpec, Block{B}}} where B","page":"External Reference","text":"ConvertFromBlock (§5.2.1): blockdecode, then project each element (no scale division).\n\n\n\n\n\n","title":"ByteFloats.ConvertFromBlock"},{"category":"method","location":"external_reference/#ByteFloats.ConvertToBlock-Union{Tuple{B}, Tuple{Type{<:Binary}, Type{<:Binary}, ProjSpec, NTuple{B, var\"#s43\"} where var\"#s43\"<:Binary, Binary}} where B","page":"External Reference","text":"ConvertToBlock (§5.2.2): decode elements, BlockProject against the supplied scale.\n\n\n\n\n\n","title":"ByteFloats.ConvertToBlock"},{"category":"method","location":"external_reference/#ByteFloats.ConvertToBlockMaxAbsFinite-Union{Tuple{B}, Tuple{Type{<:Binary}, Type{<:Binary}, ProjSpec, ProjSpec, NTuple{B, var\"#s43\"} where var\"#s43\"<:Binary}} where B","page":"External Reference","text":"ConvertToBlockMaxAbsFinite (§5.2.3): S = reduce(ωMaximumFinite, [NaN, |X₁|…]), project S under ρs, then BlockProject the elements under ρ.\n\n\n\n\n\n","title":"ByteFloats.ConvertToBlockMaxAbsFinite"},{"category":"method","location":"external_reference/#ByteFloats.DefaultAccumulatorType-Tuple{}","page":"External Reference","text":"DefaultAccumulatorType() -> Type{<:AbstractFloat}\nDefaultAccumulatorType!(T::Type{<:AbstractFloat}) -> T\n\nThe session's default accumulator carrier for wide-precision work (reductions, dot products). Initialized to binary32 (the exported IEEE 754 alias for Float32). Any AbstractFloat type is accepted (binary64, Float128, BigFloat, …).\n\n\n\n\n\n","title":"ByteFloats.DefaultAccumulatorType"},{"category":"method","location":"external_reference/#ByteFloats.DefaultProjection-Tuple{}","page":"External Reference","text":"DefaultProjection() -> ProjSpec\nDefaultProjection!(ρ::ProjSpec) -> ρ\nDefaultProjection!(m, s) -> ProjSpec\n\nThe session's default projection specification. Initialized to (DefaultRoundingMode(), DefaultSaturationMode()) = RNE_SatNone. Setting it directly decomposes ρ into DefaultRoundingMode and DefaultSaturationMode, so the three stay coherent in both directions.\n\n\n\n\n\n","title":"ByteFloats.DefaultProjection"},{"category":"method","location":"external_reference/#ByteFloats.DefaultRNG-Tuple{}","page":"External Reference","text":"DefaultRNG() -> Type{<:AbstractRNG} | AbstractRNG\nDefaultRNG!(rng) -> rng\n\nThe session's default random-number generator for stochastic rounding. Initialized to the Xoshiro type (a fresh generator per use); may be set to an AbstractRNG instance for a reproducible stream.\n\n\n\n\n\n","title":"ByteFloats.DefaultRNG"},{"category":"method","location":"external_reference/#ByteFloats.DefaultRbits-Tuple{}","page":"External Reference","text":"DefaultRbits() -> Int\nDefaultRbits!(n::Int) -> n\n\nThe session's default random-bit budget N for the stochastic rounding families (StochasticA{N}, StochasticB{N}, StochasticC{N}). Initialized to 8; must satisfy 1 ≤ N ≤ 60.\n\n\n\n\n\n","title":"ByteFloats.DefaultRbits"},{"category":"method","location":"external_reference/#ByteFloats.DefaultReturnType-Tuple{}","page":"External Reference","text":"DefaultReturnType() -> Type{<:Binary}\nDefaultReturnType!(T::Type{<:Binary}) -> T\n\nThe session's default result format — the format an operation projects into when the caller does not name one. Initialized to Binary8p2se. Independent of DefaultType, which is the default operand format.\n\n\n\n\n\n","title":"ByteFloats.DefaultReturnType"},{"category":"method","location":"external_reference/#ByteFloats.DefaultRoundingMode-Tuple{}","page":"External Reference","text":"DefaultRoundingMode() -> RoundingMode3109\nDefaultRoundingMode!(m) -> m\n\nThe session's default rounding mode. Initialized to NearestTiesToEven(). Setting it rebuilds DefaultProjection from the new mode and the current DefaultSaturationMode. Accepts an instance or a fully-parameterized type (NearestTiesToAway, StochasticA{8}, …).\n\n\n\n\n\n","title":"ByteFloats.DefaultRoundingMode"},{"category":"method","location":"external_reference/#ByteFloats.DefaultSaturationMode-Tuple{}","page":"External Reference","text":"DefaultSaturationMode() -> SaturationMode\nDefaultSaturationMode!(s) -> s\n\nThe session's default saturation mode. Initialized to SatNone(). Setting it rebuilds DefaultProjection from the current DefaultRoundingMode and the new mode. Accepts an instance or a type.\n\n\n\n\n\n","title":"ByteFloats.DefaultSaturationMode"},{"category":"method","location":"external_reference/#ByteFloats.NextGreaterThan-Union{Tuple{T}, Tuple{EXT}, Tuple{SGN}, Tuple{P}, Tuple{K}} where {K, P, SGN, EXT, T<:Binary{K, P, SGN, EXT}}","page":"External Reference","text":"NextGreaterThan(v) (draft §4.16): the least datum greater than v in the total order — one step up the code lattice, with NaN → NaN and MaxFinite/+Inf → NaN at the top. Base.nextfloat on Binary is this operation.\n\n\n\n\n\n","title":"ByteFloats.NextGreaterThan"},{"category":"method","location":"external_reference/#ByteFloats.NextLessThan-Union{Tuple{T}, Tuple{EXT}, Tuple{SGN}, Tuple{P}, Tuple{K}} where {K, P, SGN, EXT, T<:Binary{K, P, SGN, EXT}}","page":"External Reference","text":"NextLessThan(v) (draft §4.16): the greatest datum less than v in the total order — one step down the code lattice, with NaN → NaN and MinFinite/−Inf → NaN at the bottom. Base.prevfloat on Binary is this operation.\n\n\n\n\n\n","title":"ByteFloats.NextLessThan"},{"category":"function","location":"external_reference/#ByteFloats.RoundOf","page":"External Reference","text":"Draft-named accessor: RoundOf(ρ) — the rounding mode of ρ (§4.2).\n\n\n\n\n\n","title":"ByteFloats.RoundOf"},{"category":"function","location":"external_reference/#ByteFloats.SatOf","page":"External Reference","text":"Draft-named accessor: SatOf(ρ) — the saturation mode of ρ (§4.2).\n\n\n\n\n\n","title":"ByteFloats.SatOf"},{"category":"method","location":"external_reference/#ByteFloats.TotalOrder-Union{Tuple{T}, Tuple{T, T}} where T<:Binary","page":"External Reference","text":"TotalOrder⟨fx,fy⟩ (draft §4.12.1): x ≤ y in the total order (single NaN largest). Same-format comparisons run on the integer order key (bitops plan K1) — proven equivalent to the decode order exhaustively over all pairs; cross-format keys are not comparable, so mixed formats keep the decode path.\n\n\n\n\n\n","title":"ByteFloats.TotalOrder"},{"category":"method","location":"external_reference/#ByteFloats.approx-Tuple{Symbol}","page":"External Reference","text":"Retrieve a registered approximate implementation (callable via .fn).\n\n\n\n\n\n","title":"ByteFloats.approx"},{"category":"method","location":"external_reference/#ByteFloats.bitwidth-Union{Tuple{Type{Binary{K, P, S, E}}}, Tuple{E}, Tuple{S}, Tuple{P}, Tuple{K}} where {K, P, S, E}","page":"External Reference","text":"Format bitwidth K (3–8).\n\n\n\n\n\n","title":"ByteFloats.bitwidth"},{"category":"method","location":"external_reference/#ByteFloats.codedistance-Union{Tuple{T}, Tuple{T, T}} where T<:Binary","page":"External Reference","text":"Code-point distance along the total order between two values of one format.\n\n\n\n\n\n","title":"ByteFloats.codedistance"},{"category":"method","location":"external_reference/#ByteFloats.conformance-Tuple{}","page":"External Reference","text":"conformance() -> ConformanceDeclaration\n\nThe package's draft-§4.6-style conformance declaration, derived live from the operation registry, the table cache (the specializations actually instantiated), and the approximate-implementation registry. Serialize with conformance_dict or render with conformance_report.\n\n\n\n\n\n","title":"ByteFloats.conformance"},{"category":"function","location":"external_reference/#ByteFloats.conformance_dict","page":"External Reference","text":"Nested Dict{String,Any} form of the declaration — serializable by any JSON/TOML writer.\n\n\n\n\n\n","title":"ByteFloats.conformance_dict"},{"category":"function","location":"external_reference/#ByteFloats.conformance_report","page":"External Reference","text":"Human-readable conformance report.\n\n\n\n\n\n","title":"ByteFloats.conformance_report"},{"category":"method","location":"external_reference/#ByteFloats.decode-Union{Tuple{Binary{K, P, SGN, EXT}}, Tuple{EXT}, Tuple{SGN}, Tuple{P}, Tuple{K}} where {K, P, SGN, EXT}","page":"External Reference","text":"decode(v::Binary) -> Float64\n\nωDecode (draft §4.7.2). Float64 is the universal exact carrier for K ≤ 8 datums (≤ 8-bit significands, |exponent| ≲ 2^7); exactness is asserted by exhaustive test.\n\nImplemented as a constant-tuple lookup (bitops plan K2): the per-format table is generated once from _decode_compute above, so the two are correct by construction and asserted equivalent exhaustively; constant inputs still fold.\n\n\n\n\n\n","title":"ByteFloats.decode"},{"category":"method","location":"external_reference/#ByteFloats.empty_tables!-Tuple{}","page":"External Reference","text":"Drop every cached table and adaptive counter (they rebuild lazily on next use).\n\n\n\n\n\n","title":"ByteFloats.empty_tables!"},{"category":"method","location":"external_reference/#ByteFloats.expbias-Union{Tuple{Type{Binary{K, P, S, E}}}, Tuple{E}, Tuple{S}, Tuple{P}, Tuple{K}} where {K, P, S, E}","page":"External Reference","text":"Exponent bias: 2^(K−P−1) signed, 2^(K−P) unsigned.\n\n\n\n\n\n","title":"ByteFloats.expbias"},{"category":"method","location":"external_reference/#ByteFloats.expbitwidth-Union{Tuple{Type{Binary{K, P, S, E}}}, Tuple{E}, Tuple{S}, Tuple{P}, Tuple{K}} where {K, P, S, E}","page":"External Reference","text":"Width of the exponent field in bits: (K − signbit) − (P − 1).\n\n\n\n\n\n","title":"ByteFloats.expbitwidth"},{"category":"method","location":"external_reference/#ByteFloats.formatname-Union{Tuple{Type{Binary{K, P, S, E}}}, Tuple{E}, Tuple{S}, Tuple{P}, Tuple{K}} where {K, P, S, E}","page":"External Reference","text":"formatname(T) — the draft §3.2 name of a format type.\n\n\n\n\n\n","title":"ByteFloats.formatname"},{"category":"method","location":"external_reference/#ByteFloats.ftz_variant-Tuple{Symbol, Type{<:Binary}, Type{<:Binary}, ProjSpec}","page":"External Reference","text":"ftz_variant(op, fr, f1, ρ) -> fn\n\nBuild the Annex-style approximate unary implementation: compute the defined result, then flush subnormal results to zero or MinNormalOf(fr), whichever is nearer, ties toward zero (magnitude-symmetric for signed formats). Returned fn is suitable for register_approx!; for fr with precision P its κ is 2^(P-2) when P ≥ 2 (largest flushed-to-zero subnormal) and 0 when P = 1 (no subnormals exist).\n\n\n\n\n\n","title":"ByteFloats.ftz_variant"},{"category":"method","location":"external_reference/#ByteFloats.isextended-Union{Tuple{Type{Binary{K, P, S, E}}}, Tuple{E}, Tuple{S}, Tuple{P}, Tuple{K}} where {K, P, S, E}","page":"External Reference","text":"Whether the format's domain is Extended (datum set includes infinities).\n\n\n\n\n\n","title":"ByteFloats.isextended"},{"category":"method","location":"external_reference/#ByteFloats.issigned-Union{Tuple{Type{Binary{K, P, S, E}}}, Tuple{E}, Tuple{S}, Tuple{P}, Tuple{K}} where {K, P, S, E}","page":"External Reference","text":"Whether the format is Signed (has a sign bit and negative datums).\n\n\n\n\n\n","title":"ByteFloats.issigned"},{"category":"method","location":"external_reference/#ByteFloats.list_approx-Tuple{}","page":"External Reference","text":"Sorted names of all registered κ-approximate implementations.\n\n\n\n\n\n","title":"ByteFloats.list_approx"},{"category":"method","location":"external_reference/#ByteFloats.measure_kappa-Union{Tuple{N}, Tuple{F}, Tuple{F, Symbol, Type{<:Binary}, NTuple{N, DataType}, ProjSpec}} where {F, N}","page":"External Reference","text":"measure_kappa(fn, op, fr, argformats, ρ; max_exhaustive=2^22, rng, samples=2^20)\n    -> (κ::Float64, exhaustive::Bool)\n\nMeasure κ of fn(x::argformats[1], …)::fr against the defined results of draft operation op under ρ. Enumerates the full input cross-product when it has at most max_exhaustive points (always true for arity ≤ 2); otherwise measures on samples uniform draws and reports exhaustive = false.\n\n\n\n\n\n","title":"ByteFloats.measure_kappa"},{"category":"method","location":"external_reference/#ByteFloats.nrandbits-Tuple{Type{<:RoundingMode3109}}","page":"External Reference","text":"Number of random bits N consumed per projected element; 0 for pure modes.\n\n\n\n\n\n","title":"ByteFloats.nrandbits"},{"category":"method","location":"external_reference/#ByteFloats.projmode-Tuple{RoundingMode{:Nearest}}","page":"External Reference","text":"Translate a Base.RoundingMode to its RoundingMode3109 counterpart (identity on the latter). Used only at API boundaries. Internals always carry RoundingMode3109.\n\n\n\n\n\n","title":"ByteFloats.projmode"},{"category":"method","location":"external_reference/#ByteFloats.rawvalue-Union{Tuple{EXT}, Tuple{SGN}, Tuple{P}, Tuple{K}, Tuple{Type{Binary{K, P, SGN, EXT}}, UInt8}} where {K, P, SGN, EXT}","page":"External Reference","text":"Unsafe raw constructor used by kernels after invariants are established.\n\n\n\n\n\n","title":"ByteFloats.rawvalue"},{"category":"method","location":"external_reference/#ByteFloats.register_approx!-Union{Tuple{N}, Tuple{F}, Tuple{Symbol, Symbol, Type{<:Binary}, NTuple{N, DataType}, ProjSpec, F}} where {F, N}","page":"External Reference","text":"register_approx!(name, op, fr, argformats, ρ, fn; κ=nothing, kwargs...) -> ApproxImpl\n\nRegister fn as a named κ-approximate implementation of op⟨argformats → fr, ρ⟩. κ is measured by enumeration (see measure_kappa); a declared κ smaller than the measured value is rejected. Omitting κ declares the measured value. NaN-κ implementations (non-finite mismatches) may be registered only by declaring κ=NaN.\n\n\n\n\n\n","title":"ByteFloats.register_approx!"},{"category":"method","location":"external_reference/#ByteFloats.table_bytes-Tuple{}","page":"External Reference","text":"Total bytes currently held by the table caches (unary/binary + ternary).\n\n\n\n\n\n","title":"ByteFloats.table_bytes"},{"category":"method","location":"external_reference/#ByteFloats.trailingsigbits-Union{Tuple{Type{Binary{K, P, S, E}}}, Tuple{E}, Tuple{S}, Tuple{P}, Tuple{K}} where {K, P, S, E}","page":"External Reference","text":"Trailing-significand width P − 1 (the stored fraction bits).\n\n\n\n\n\n","title":"ByteFloats.trailingsigbits"},{"category":"method","location":"external_reference/#ByteFloats.unregister_approx!-Tuple{Symbol}","page":"External Reference","text":"Remove a registered κ-approximate implementation (no-op if absent).\n\n\n\n\n\n","title":"ByteFloats.unregister_approx!"},{"category":"function","location":"external_reference/#ByteFloats.vmap!","page":"External Reference","text":"vmap!(dest, Val(op), fr, ρ, A[, B[, C]]; rng) -> dest\nvmap(op, fr, ρ, A...; rng)\n\nElementwise draft operation over arrays of Binary values, projecting into fr under ρ. Pure ρ runs the Shape-A gather whenever a table exists or the ternary bitwidth policy grants one; stochastic specializations (and untabled ternary signatures) run the scalar path per element (each element drawing its own R).\n\n\n\n\n\n","title":"ByteFloats.vmap!"},{"category":"method","location":"external_reference/#ByteFloats.vmap-Union{Tuple{F}, Tuple{Symbol, Type{<:Binary}, ProjSpec, PackedVector{F}}} where F<:Binary","page":"External Reference","text":"vmap(op, fr, ρ, pv::PackedVector; rng=nothing) -> Vector{fr}\n\nElementwise op over packed storage: unpack a 256-element tile into a byte scratch, run the ordinary byte kernel (vmap!), emit, repeat. Never computes on packed words directly — the deliberate compute-unpacked/store-packed boundary.\n\n\n\n\n\n","title":"ByteFloats.vmap"},{"category":"function","location":"external_reference/#ByteFloats.with_default_accumulatortype","page":"External Reference","text":"with_default_type(f, args...)          -> f(DefaultType(), args...)\nwith_default_returntype(f, args...)    -> f(DefaultReturnType(), args...)\nwith_default_accumulatortype(f, args...) -> f(DefaultAccumulatorType(), args...)\nwith_default_projection(f, args...)    -> f(DefaultProjection(), args...)\n\nCall f with the named session default as its first argument — the supported way to consume a default. While the default still holds its initial value (the overwhelmingly common case) the call is statically compiled against that constant: no dynamic dispatch, f fully specialized. After the default is changed, the call crosses a function barrier instead: one dynamic dispatch, everything inside fully specialized.\n\nAllocation contract: the combinator's return type unions both paths. When f's result type does not depend on the default — e.g. with_default_projection with the formats fixed by the caller — the union is concrete and the call is zero-allocation with a concretely inferred result. When the result's type is the default (with_default_type((T, x) -> T(x), …)), the value is computed on the specialized path but boxes once at escape — the irreducible cost of a runtime-chosen type.\n\njulia> with_default_type((T, x) -> T(x), 1.5)\nBinary8p2se(1.5 ≡ 0x41)\n\njulia> with_default_projection((ρ, x, y) -> Add(Binary8p4se, ρ, x, y),\n                               Binary8p4se(1.5), Binary8p4se(0.25))\nBinary8p4se(1.75 ≡ 0x46)\n\n\n\n\n\n","title":"ByteFloats.with_default_accumulatortype"},{"category":"function","location":"external_reference/#ByteFloats.with_default_projection","page":"External Reference","text":"with_default_type(f, args...)          -> f(DefaultType(), args...)\nwith_default_returntype(f, args...)    -> f(DefaultReturnType(), args...)\nwith_default_accumulatortype(f, args...) -> f(DefaultAccumulatorType(), args...)\nwith_default_projection(f, args...)    -> f(DefaultProjection(), args...)\n\nCall f with the named session default as its first argument — the supported way to consume a default. While the default still holds its initial value (the overwhelmingly common case) the call is statically compiled against that constant: no dynamic dispatch, f fully specialized. After the default is changed, the call crosses a function barrier instead: one dynamic dispatch, everything inside fully specialized.\n\nAllocation contract: the combinator's return type unions both paths. When f's result type does not depend on the default — e.g. with_default_projection with the formats fixed by the caller — the union is concrete and the call is zero-allocation with a concretely inferred result. When the result's type is the default (with_default_type((T, x) -> T(x), …)), the value is computed on the specialized path but boxes once at escape — the irreducible cost of a runtime-chosen type.\n\njulia> with_default_type((T, x) -> T(x), 1.5)\nBinary8p2se(1.5 ≡ 0x41)\n\njulia> with_default_projection((ρ, x, y) -> Add(Binary8p4se, ρ, x, y),\n                               Binary8p4se(1.5), Binary8p4se(0.25))\nBinary8p4se(1.75 ≡ 0x46)\n\n\n\n\n\n","title":"ByteFloats.with_default_projection"},{"category":"function","location":"external_reference/#ByteFloats.with_default_returntype","page":"External Reference","text":"with_default_type(f, args...)          -> f(DefaultType(), args...)\nwith_default_returntype(f, args...)    -> f(DefaultReturnType(), args...)\nwith_default_accumulatortype(f, args...) -> f(DefaultAccumulatorType(), args...)\nwith_default_projection(f, args...)    -> f(DefaultProjection(), args...)\n\nCall f with the named session default as its first argument — the supported way to consume a default. While the default still holds its initial value (the overwhelmingly common case) the call is statically compiled against that constant: no dynamic dispatch, f fully specialized. After the default is changed, the call crosses a function barrier instead: one dynamic dispatch, everything inside fully specialized.\n\nAllocation contract: the combinator's return type unions both paths. When f's result type does not depend on the default — e.g. with_default_projection with the formats fixed by the caller — the union is concrete and the call is zero-allocation with a concretely inferred result. When the result's type is the default (with_default_type((T, x) -> T(x), …)), the value is computed on the specialized path but boxes once at escape — the irreducible cost of a runtime-chosen type.\n\njulia> with_default_type((T, x) -> T(x), 1.5)\nBinary8p2se(1.5 ≡ 0x41)\n\njulia> with_default_projection((ρ, x, y) -> Add(Binary8p4se, ρ, x, y),\n                               Binary8p4se(1.5), Binary8p4se(0.25))\nBinary8p4se(1.75 ≡ 0x46)\n\n\n\n\n\n","title":"ByteFloats.with_default_returntype"},{"category":"method","location":"external_reference/#ByteFloats.with_default_type-Union{Tuple{F}, Tuple{F, Vararg{Any}}} where F","page":"External Reference","text":"with_default_type(f, args...)          -> f(DefaultType(), args...)\nwith_default_returntype(f, args...)    -> f(DefaultReturnType(), args...)\nwith_default_accumulatortype(f, args...) -> f(DefaultAccumulatorType(), args...)\nwith_default_projection(f, args...)    -> f(DefaultProjection(), args...)\n\nCall f with the named session default as its first argument — the supported way to consume a default. While the default still holds its initial value (the overwhelmingly common case) the call is statically compiled against that constant: no dynamic dispatch, f fully specialized. After the default is changed, the call crosses a function barrier instead: one dynamic dispatch, everything inside fully specialized.\n\nAllocation contract: the combinator's return type unions both paths. When f's result type does not depend on the default — e.g. with_default_projection with the formats fixed by the caller — the union is concrete and the call is zero-allocation with a concretely inferred result. When the result's type is the default (with_default_type((T, x) -> T(x), …)), the value is computed on the specialized path but boxes once at escape — the irreducible cost of a runtime-chosen type.\n\njulia> with_default_type((T, x) -> T(x), 1.5)\nBinary8p2se(1.5 ≡ 0x41)\n\njulia> with_default_projection((ρ, x, y) -> Add(Binary8p4se, ρ, x, y),\n                               Binary8p4se(1.5), Binary8p4se(0.25))\nBinary8p4se(1.75 ≡ 0x46)\n\n\n\n\n\n","title":"ByteFloats.with_default_type"},{"category":"section","location":"julia_compatibility/#Julia-Compatibility","page":"Julia Compatibility","text":"Binary values speak ordinary Julia. The Base register (src/juliacompat.jl) maps every draft operation that has a Base counterpart onto its Base name, so generic Julia code — and your fingers — can use the spellings they already know. Every method there is exactly one same-format spec-register call under the session default projection; there is no third semantics: x + y is Add(x, y), which is Add(T, DefaultProjection(), x, y).","title":"Julia Compatibility"},{"category":"section","location":"julia_compatibility/#Using-the-overloads","page":"Julia Compatibility","text":"julia> x, y, z = Binary8p4se(1.6), Binary8p4se(0.25), Binary8p4se(2.0);\n\njulia> x + y                       # Add\nBinary8p4se(1.875 ≡ 0x47)\n\njulia> exp(y)                      # Exp — a 256-byte table lookup once warm\nBinary8p4se(1.25 ≡ 0x42)\n\njulia> atan(y, x)                  # ArcTan2, in Base's (y, x) argument order\nBinary8p4se(0.15625 ≡ 0x2a)\n\njulia> fma(x, y, z)                # FMA — one rounding; muladd is the same op\nBinary8p4se(2.5 ≡ 0x4a)\n\njulia> sincos(y)                   # composite: (Sin(y), Cos(y)) componentwise\n(Binary8p4se(0.25 ≡ 0x30), Binary8p4se(1.0 ≡ 0x40))\n\nBecause the veneers follow the session default projection, changing it changes them (see the session-defaults section of the User Guide):\n\njulia> Binary8p4se(200.0) * Binary8p4se(2.0)     # RNE_SatNone: overflow → Inf\nBinary8p4se(Inf ≡ 0x7f)\n\njulia> DefaultProjection!(RTZ_SatFinite);\n\njulia> Binary8p4se(200.0) * Binary8p4se(2.0)     # now clamps to MaxFinite\nBinary8p4se(224.0 ≡ 0x7e)\n\nCode that must be insensitive to the session default names its projection: Multiply(T, RNE_SatNone, x, y).","title":"Using the overloads"},{"category":"section","location":"julia_compatibility/#The-mapping","page":"Julia Compatibility","text":"Base spelling draft operation\n+ - * /, unary - Add, Subtract, Multiply, Divide, Negate\nabs, inv, sqrt Abs, Recip, Sqrt\nexp, exp2, expm1, log, log2, log1p Exp, Exp2, ExpMinusOne, Log, Log2, LogOnePlus\nsin, cos, tan, asin, acos, atan Sin, Cos, Tan, ArcSin, ArcCos, ArcTan\nsinh, cosh, tanh, asinh, acosh, atanh Sinh, Cosh, Tanh, ArcSinh, ArcCosh, ArcTanh\nsinpi, cospi, tanpi SinPi, CosPi, TanPi\natan(y, x) ArcTan2 (Base's argument order)\ncopysign, hypot CopySign, Hypot\nmin, max Minimum, Maximum (NaN-propagating, exactly Base's float semantics)\nfma, muladd FMA (both: one rounding)\nclamp Clamp\nsincos, sincospi, minmax componentwise composites of the draft ops\n\nThe mapping is a declarative partition over the op lists, and the test suite asserts it is exhaustive: every operation is either mapped above or listed in _NO_BASE_COUNTERPART. Adding an operation to the registry forces an explicit decision here.","title":"The mapping"},{"category":"section","location":"julia_compatibility/#What-is-*not*-mapped,-and-why","page":"Julia Compatibility","text":"Draft operations with no Base spelling — call them by their draft names: RSqrt, Softplus, ArcSinPi/ArcCosPi/ArcTanPi/ArcTan2Pi (Base has only the sinpi family), the NaN-ignoring/magnitude/finite extremum families (MinimumNumber, MaximumMagnitude, MinimumFinite, …; Base has no NaN-ignoring pair), and FAA (no Base fused add-add).\n\njulia> min(Binary8p4se(NaN), x)          # Base semantics: NaN propagates\nBinary8p4se(NaN ≡ 0x80)\n\njulia> MinimumNumber(Binary8p4se(NaN), x)   # the NaN-ignoring draft op\nBinary8p4se(1.625 ≡ 0x45)\n\nMixed Binary formats — deliberately a promotion ... failed to change error, never a silent widening. Mixing formats is an explicit Convert:\n\njulia> x + Convert(Binary8p4se, RNE_SatNone, Binary5p3sf(1.0))\nBinary8p4se(2.5 ≡ 0x4a)\n\nBinary with ordinary numbers — promotes to Float64 (the exact carrier) through the rules in formats.jl, so the result is a Float64, not a re-projected Binary:\n\njulia> x + 2.0\n3.625\n\nProject it back explicitly when that is what you mean: Convert(Binary8p4se, RNE_SatNone, decode(x) + 2.0).","title":"What is not mapped, and why"},{"category":"section","location":"julia_compatibility/#The-rest-of-the-Base-surface","page":"Julia Compatibility","text":"Beyond arithmetic, Binary integrates where the other layers provide it: predicates and constants (isnan, isfinite, signbit, zero, one, eps, typemin/typemax, floatmin/floatmax), comparisons and isless on integer order keys, sort via an O(n) counting sort, nextfloat/prevfloat as the draft Next operations, codepoint, and rand/randn (see the User Guide). All of it goes through the same projection engine and decode tables as the draft-named API.","title":"The rest of the Base surface"},{"category":"section","location":"introduction/#Introduction","page":"Introduction","text":"ByteFloats.jl is a conforming, performance-oriented Julia implementation of the IEEE P3109 draft standard — Arithmetic Formats for Machine Learning — covering every binary format the draft defines at bitwidths 3 through 8, every projection (rounding × saturation) mode including the three stochastic families, the full scalar operation catalog, block-scaled (\"MX-style\") operations, and the draft's conformance and κ-approximation machinery.","title":"Introduction"},{"category":"section","location":"introduction/#Why-this-package-exists","page":"Introduction","text":"Small floating-point formats are easy to implement approximately and surprisingly hard to implement exactly. An FP8 Exp differs from a bit-exact one only in a handful of code points near rounding boundaries — invisible in a demo, decisive in a conformance suite, and quietly corrosive in research that compares number formats. ByteFloats.jl takes the position that for value sets this small there is no excuse for approximation:\n\nBit-exact defined results on every default path. Every operation's result is the correct projection of the mathematically exact value, established through a rigorous oracle (exact arithmetic, correctly-rounded Float128 where IEEE mandates it, and MPFR directed-rounding enclosures with automatic precision escalation everywhere else).\nOne projection engine. RoundToPrecision → Saturate → Encode is the single write path into a code point. There is no second, \"fast but slightly different\" rounding routine anywhere in the package.\nApproximation is opt-in, named, and measured. Faster-but-inexact implementations live behind an explicit registry (register_approx!); each registration's deviation bound κ is measured by exhaustive enumeration at registration time, and understated declarations are rejected. Nothing approximate is reachable from the default API.\nExhaustively tested. The value sets are tiny (≤ 256 points per format), so the test suite does not sample — it enumerates: every operation on every input, every ordering of every pair, every stochastic draw at boundary budgets. The shipped suite carries ≈ 8.9 million assertions.\nFast where it matters. Pure-mode elementwise work runs through precomputed lookup tables (sub-nanosecond per element); the scalar path is fully specialized and allocation-free (≈ 18 ns for a complete Add including projection); sub-byte packed storage, integer-keyed ordering with an O(n) counting sort, and a mask-based rounding core round out the performance story. A reproducible Chairmarks benchmark suite ships in benchmarking/.","title":"Why this package exists"},{"category":"section","location":"introduction/#Thirty-second-tour","page":"Introduction","text":"julia> using ByteFloats\n\njulia> x = Binary8p4se(1.6)          # construct = project under the default spec\nBinary8p4se(1.625 ≡ 0x45)\n\njulia> x + Binary8p4se(0.25)         # Base operators use the format's default spec\nBinary8p4se(1.875 ≡ 0x47)\n\njulia> Add(Binary8p4se, RTP_SatNone, x, Binary8p4se(0.25))\nBinary8p4se(1.875 ≡ 0x47)            # spec-named register: any mode, explicitly\n\njulia> σ = RSA_SatNone();            # stochastic rounding, default N = 8 bits\n\njulia> Add(Binary8p4se, σ, Binary8p4se(2.0), Binary8p4se(0.03125); R = 255)\nBinary8p4se(2.25 ≡ 0x49)             # explicit draw R makes it reproducible\n\njulia> Exp(Binary8p4se, RNE_SatNone, Binary8p4se.([-1.0, 0.5, 2.0]))\n3-element Vector{Binary8p4se}:       # array methods route through 256-byte tables\n Binary8p4se(0.375 ≡ 0x34)\n Binary8p4se(1.625 ≡ 0x45)\n Binary8p4se(7.5 ≡ 0x57)","title":"Thirty-second tour"},{"category":"section","location":"introduction/#The-formats","page":"Introduction","text":"A format is the type Binary{K,P,SGN,EXT}: bitwidth K ∈ 3:8, precision P ∈ 1:K (with the signedness constraint), signed/unsigned SGN, and extended (has ±Inf) or finite EXT. All 120 draft formats carry their draft names as exported aliases — Binary8p4se is Binary{8,4,true,true} (\"K = 8, P = 4, signed, extended\"); the trailing letters are s/u for signedness and e/f for extended/finite. Values are 1-byte immutable wrappers around their code point; every format has a single NaN, no negative zero, and Float64 serves as the exact interchange carrier for all datums.","title":"The formats"},{"category":"section","location":"introduction/#Where-to-go-next","page":"Introduction","text":"User Guide — the complete tour of the public API: formats and values, projection specifications, the two operation registers, arrays and sorting, blocks and scaled operations, packed storage, conversion, conformance, and performance guidance.\nUser Examples — worked, runnable examples in three tiers: basic usage, machine-learning quantization workflows, and deep-learning block/kernel patterns.\nTechnical Guide — the internals: the encoding and projection engine, the oracle's evaluation protocol and its two rigor classes, tables and kernels, the block layer's exactness filters, and the testing and benchmarking doctrine.\nTechnical Examples — internals-level recipes: pipeline introspection, verifying custom kernels against the oracle, κ measurement, differential builds, and doctrine-compliant benchmarking.","title":"Where to go next"},{"category":"section","location":"introduction/#Installation","page":"Introduction","text":"The package is not yet registered, so install it by URL:\n\npkg> add https://github.com/JeffreySarnoff/ByteFloats.jl\n\nAppend #main (or a tag/commit) to pin a revision. For an editable install, pkg> dev https://github.com/JeffreySarnoff/ByteFloats.jl clones into ~/.julia/dev, or pkg> dev path/to/ByteFloats.jl uses an existing clone.\n\nRequires Julia 1.12. Quadmath (libquadmath's Float128) is a hard dependency used only inside the oracle and exact-fallback paths; on platforms where it misbehaves, ENV[\"ByteFloats_Float128\"] = \"disable\" before loading selects the pure-MPFR configuration with identical results (this equivalence is itself part of the test suite).","title":"Installation"},{"category":"section","location":"introduction/#Status-and-conformance","page":"Introduction","text":"conformance() returns the live, serializable conformance declaration (formats, operations, modes, instantiated table specializations, and κ registrations); ByteFloats.draft_revision() names the implemented draft revision. Interpretations made where the draft text under-determined behavior are marked [interp] in the source comments.","title":"Status and conformance"},{"category":"section","location":"user_examples/#User-Examples","page":"User Examples","text":"Worked, runnable examples. Every output shown was captured from a real session; seeds are fixed so you can reproduce them exactly.","title":"User Examples"},{"category":"section","location":"user_examples/#Basic","page":"User Examples","text":"","title":"Basic"},{"category":"section","location":"user_examples/#One-value,-every-rounding-mode","page":"User Examples","text":"The value 2.30078125 sits between the Binary8p4se grid points 2.25 and 2.5 (ulp = 0.25 in [2, 4)):\n\nusing ByteFloats\n\nfor μ in (NearestTiesToEven(), NearestTiesToAway(), TowardPositive(),\n          TowardNegative(), TowardZero(), ToOdd())\n    v = Convert(Binary8p4se, ProjSpec(μ, SatNone()), 2.30078125)\n    println(rpad(string(typeof(μ)), 20), \" → \", decode(v))\nend\n\nNearestTiesToEven    → 2.25\nNearestTiesToAway    → 2.25\nTowardPositive       → 2.5\nTowardNegative       → 2.25\nTowardZero           → 2.25\nToOdd                → 2.25\n\n(ToOdd keeps 2.25 because its significand is already odd; try 2.05 to see it move.)","title":"One value, every rounding mode"},{"category":"section","location":"user_examples/#Enumerating-a-whole-format","page":"User Examples","text":"Small formats are small enough to look at in full — 16 code points for Binary4p2se, sorted by the total order (single NaN last):\n\ndecode.(sort(Binary4p2se.(0x00:0x0f)))      # broadcast the code-point constructor\n\n[-Inf, -2.0, -1.5, -1.0, -0.75, -0.5, -0.25, 0.0,\n  0.25, 0.5, 0.75, 1.0, 1.5, 2.0, Inf, NaN]\n\nThis is a good way to build intuition for a format before committing to it: you can see the subnormal spacing, the binade structure, and the dynamic range.","title":"Enumerating a whole format"},{"category":"section","location":"user_examples/#Saturation-in-one-line-each","page":"User Examples","text":"julia> w, two = Binary8p4se(200.0), Binary8p4se(2.0);   # MaxFinite is 224\n\njulia> Multiply(Binary8p4se, RNE_SatNone, w, two)\nBinary8p4se(Inf ≡ 0x7f)\n\njulia> Multiply(Binary8p4se, RNE_SatFinite, w, two)\nBinary8p4se(224.0 ≡ 0x7e)","title":"Saturation in one line each"},{"category":"section","location":"user_examples/#Random-values-under-a-chosen-projection","page":"User Examples","text":"rand and randn take a projection keyword on their scalar ::Type forms. One underlying Float64 draw, landed on the format's grid four different ways — the seed fixes the draw, the projection decides the landing:\n\njulia> rand(Xoshiro(2), Float64)                # the underlying uniform draw\n0.0022505868897625403\n\njulia> rand(Xoshiro(2), Binary8p4se)            # default: floor (stays in [0,1))\nBinary8p4se(0.001953125 ≡ 0x02)\n\njulia> rand(Xoshiro(2), Binary8p4se; projection = RTP_SatNone)   # ceiling\nBinary8p4se(0.0029296875 ≡ 0x03)\n\njulia> rand(Xoshiro(2), Binary8p4se; projection = RNE_SatNone)   # nearest\nBinary8p4se(0.001953125 ≡ 0x02)\n\njulia> rand(Xoshiro(2), Binary8p4se; projection = RSA_SatNone(8))  # stochastic\nBinary8p4se(0.001953125 ≡ 0x02)\n\nA stochastic projection draws its random bits from the same rng as the uniform draw, so seeded streams stay reproducible. Note the default is the contract-keeper: a nearest or upward projection can return exactly 1.0 (mass near the top rounds up), which is why floor is the default.\n\nThe same keyword on randn — here the draw is -1.9757…:\n\njulia> randn(Xoshiro(6), Binary8p4se)           # default: nearest + SatFinite\nBinary8p4se(-2.0 ≡ 0xc8)\n\njulia> randn(Xoshiro(6), Binary8p4se; projection = RTZ_SatFinite)  # toward zero\nBinary8p4se(-1.875 ≡ 0xc7)\n\nWhere the saturation half of the default earns its keep: Binary3p1se has MaxFinite = 1.0, so normal tail draws overflow constantly. Seed 360 draws z = 3.326…:\n\njulia> randn(Xoshiro(360), Binary3p1se)         # SatFinite clamps the tail\nBinary3p1se(1.0 ≡ 0x02)\n\njulia> randn(Xoshiro(360), Binary3p1se; projection = RNE_SatNone)\nBinary3p1se(Inf ≡ 0x03)                         # opt out, get the overflow\n\nThe array and ! forms (rand(T, dims), randn!(A), …) always use the defaults; for arrays under another projection, draw scalars: [rand(rng, T; projection = ρ) for _ in 1:n].","title":"Random values under a chosen projection"},{"category":"section","location":"user_examples/#General-AI","page":"User Examples","text":"","title":"General AI"},{"category":"section","location":"user_examples/#Log-odds-belief-updating-—-and-where-tiny-formats-bite-reasoning","page":"User Examples","text":"Classic probabilistic reasoning: accumulate independent evidence as log-likelihood ratios. A sensor with hit rate 0.85 and false-alarm rate 0.30 contributes log(0.85/0.30) ≈ 1.0415 per alarm. In Binary8p3se that datum is 1.0 — and the running belief stalls exactly the way the gradient accumulator does in the Deep Learning section:\n\nusing ByteFloats\n\nfunction logodds_demo(nalarms)\n    llr = Binary8p3se(log(0.85 / 0.30))      # quantized evidence weight\n    acc = Binary8p3se(0.0)\n    for _ in 1:nalarms\n        acc = Add(Binary8p3se, RNE_SatNone, acc, llr)\n    end\n    post(l) = 1 / (1 + exp(-l))\n    exact = nalarms * log(0.85 / 0.30)\n    (decode(llr), round(exact; digits=2), decode(acc),\n     round(post(exact); digits=5), round(post(decode(acc)); digits=5))\nend\nlogodds_demo(12)\n\n(1.0, 12.5, 8.0, 1.0, 0.99966)\n# llr datum, exact log-odds, quantized log-odds, exact posterior, quantized posterior\n\nThe accumulator is wrong by a third (8.0 vs 12.5 — near 8 the ulp is 2.0, so 8 + 1 rounds straight back to 8), yet the decision is untouched: both posteriors are ≈ 1. Threshold decisions need order, not magnitude — which is why coarse formats work in reasoning pipelines far past the point where their arithmetic looks broken. When magnitude does matter, the cures are the same as for gradients: a wider accumulator format, or stochastic rounding.","title":"Log-odds belief updating — and where tiny formats bite reasoning"},{"category":"section","location":"user_examples/#Fuzzy-inference-in-an-unsigned-format","page":"User Examples","text":"Membership degrees live in [0, 1] and are never negative — a job for an unsigned finite format. Binary8p6uf gives a [0, 15.5] range with 1/32 resolution at 1; the classic fuzzy connectives are registry operations (Minimum = Gödel t-norm, Maximum = s-norm, Multiply = product t-norm):\n\nF = Binary8p6uf\ntrap(x, a, b, c, d) = clamp(min((x - a) / (b - a), 1.0, (d - x) / (d - c)), 0.0, 1.0)\nwarm = F(trap(26.0, 15, 20, 24, 28))         # membership of 26 °C in \"warm\"\nhot  = F(trap(26.0, 24, 30, 100, 101))       # … and in \"hot\"\n\n(decode(warm), decode(hot),\n decode(Minimum(F, RNE_SatNone, warm, hot)),      # warm AND hot\n decode(Maximum(F, RNE_SatNone, warm, hot)),      # warm OR hot\n decode(Multiply(F, RNE_SatFinite, warm, hot)))   # product t-norm\n\n(0.5, 0.3359375, 0.3359375, 0.5, 0.16796875)\n\nBecause every connective is a bit-exact registry op, a fuzzy rule base evaluated in Binary8p6uf is reproducible across machines to the code point — a property sampled float pipelines cannot promise.","title":"Fuzzy inference in an unsigned format"},{"category":"section","location":"user_examples/#Search-heuristics:-what-quantized-scores-keep-is-the-ordering","page":"User Examples","text":"Game-tree and beam search consume evaluation scores only through comparisons. Quantizing 200 heuristic scores to 8 bits collapses them onto 78 distinct code points — yet the argmax survives, and sorting is exact (integer order keys, O(n) counting sort, single NaN ordered last deterministically):\n\nusing Random\nrng = Xoshiro(21)\nscores = randn(rng, 200) .* 3\nq = Binary8p4se.(scores)\n\n(argmax(scores), argmax(decode.(q)), argmax(scores) == argmax(decode.(q)),\n decode.(sort(q; rev=true)[1:5]), length(unique(codepoint.(q))))\n\n(140, 140, true, [8.0, 7.5, 6.5, 6.0, 6.0], 78)\n\nThe two 6.0s are the caveat: quantization creates ties the exact scores did not have. TotalOrder's deterministic tie behavior means the search expands the same node on every run — ties change which answer you get, not whether you can reproduce it.","title":"Search heuristics: what quantized scores keep is the ordering"},{"category":"section","location":"user_examples/#Machine-Learning","page":"User Examples","text":"","title":"Machine Learning"},{"category":"section","location":"user_examples/#Quantizing-a-weight-tensor-and-measuring-the-damage","page":"User Examples","text":"using ByteFloats, Random, Statistics\n\nrng = Xoshiro(42)\nw = randn(rng, 10_000) .* 0.25          # typical trained-weight scale\nq = Binary8p4se.(w)                     # project every weight\nback = decode.(q)\n\nmean((w .- back).^2), maximum(abs.(w .- back)), count(isinf, back) / length(back)\n\n(4.41e-5, 0.0312, 0.0)                  # MSE, max |error|, overflow fraction\n\nThe max error is exactly half an ulp of the largest binade the data reached — the worst case nearest rounding permits.","title":"Quantizing a weight tensor and measuring the damage"},{"category":"section","location":"user_examples/#Picking-a-format:-precision-vs-range","page":"User Examples","text":"For the same Gaussian tensor, K = 8 formats trade significand bits against exponent range. RMSE doubles per lost significand bit; MaxFinite grows explosively:\n\nrng = Xoshiro(7); x = randn(rng, 50_000)\nfor F in (Binary8p5se, Binary8p4se, Binary8p3se, Binary8p2se)\n    back = [decode(Convert(F, RNE_SatFinite, xi)) for xi in x]\n    println(rpad(formatname(F), 12), \" rmse = \", round(sqrt(mean((x .- back).^2)); sigdigits=3),\n            \"   maxfinite = \", decode(MaxFiniteOf(F)))\nend\n\nBinary8p5se  rmse = 0.0133   maxfinite = 15.0\nBinary8p4se  rmse = 0.0266   maxfinite = 224.0\nBinary8p3se  rmse = 0.0528   maxfinite = 49152.0\nBinary8p2se  rmse = 0.103    maxfinite = 2.147483648e9\n\nFor unit-scale data, spend bits on precision; buy range only when your data needs it (or get it from a block scale — see below).","title":"Picking a format: precision vs range"},{"category":"section","location":"user_examples/#Stochastic-rounding-is-unbiased-where-nearest-is-not","page":"User Examples","text":"Nearest rounding maps 0.30078125 to 0.3125 every single time — a systematic +0.0117 bias. Stochastic rounding is right on average:\n\ntarget = 0.30078125                       # ν = 3/16 of an ulp above 0.296875\nσ = RSA_SatNone(16)                       # StochasticA{16} with SatNone\nrng = Xoshiro(1)\nm = mean(decode(Convert(Binary8p4se, σ, target; rng)) for _ in 1:200_000)\n(decode(Binary8p4se(target)), m)\n\n(0.3125, 0.300765)                        # RNE result vs stochastic mean ≈ target\n\nThis is the property that makes stochastic rounding matter for accumulating many small contributions (gradients, activations statistics) in a low-precision format.","title":"Stochastic rounding is unbiased where nearest is not"},{"category":"section","location":"user_examples/#Deep-Learning","page":"User Examples","text":"","title":"Deep Learning"},{"category":"section","location":"user_examples/#MX-style-block-quantization-—-and-the-staging-pitfall","page":"User Examples","text":"Block formats pair a shared scale with narrow elements, tracking dynamic range that a bare element format cannot. Here each row of W lives at a different scale, spanning ~2¹⁶ across the matrix:\n\nusing ByteFloats, Random, Statistics\n\nrng = Xoshiro(3)\nW = randn(rng, 64, 64) .* 2 .* (2.0 .^ (collect(0:63) ./ 4))   # per-row scales\n\nfunction mx_rows(W, ::Type{FST}, ::Type{FS}, ::Type{FE}, ::Val{B}) where {FST,FS,FE,B}\n    n, m = size(W)\n    [begin\n        # stage through a WIDE-RANGE format so nothing clamps before scaling\n        seg = ntuple(k -> Convert(FST, RNE_SatFinite, W[i, (j-1)*B + k]), Val(B))\n        ConvertToBlockMaxAbsFinite(FS, FE, RNE_SatNone, RNE_SatNone, seg)\n    end for i in 1:n, j in 1:m ÷ B]\nend\n\nblocks = mx_rows(W, Binary8p2se, Binary8p1uf, Binary8p4se, Val(32))\nrecon = [let b = blocks[i, (j-1) ÷ 32 + 1]\n             decode(b.s) * decode(b.x[(j-1) % 32 + 1])\n         end for i in 1:64, j in 1:64]\nplain = [decode(Convert(Binary8p4se, RNE_SatFinite, w)) for w in W]\n\nrelerr(A) = sqrt(mean(((W .- A) ./ max.(abs.(W), 1e-9)).^2))\nrelerr(plain), relerr(recon)\n\n(0.616, 0.109)      # plain 8p4se clamps the large rows; MX tracks them\n\nwarning: Stage wide, then block-quantize\nConvertToBlockMaxAbsFinite takes already-Binary elements. If you stage your Float64 data through the element format first, SatFinite clamps the large values before the block scale can absorb them, and blocks buy you nothing — we measured exactly that (0.617 vs 0.616) with Binary8p4se staging. Stage through a wide-range format (Binary8p2se above); the residual 0.109 here is the staging format's own precision, which bounds what any downstream scheme can keep.\n\nAt B = 32 with an 8-bit scale the storage cost is 8.25 bits/value.","title":"MX-style block quantization — and the staging pitfall"},{"category":"section","location":"user_examples/#Quantized-dot-products-with-one-final-rounding","page":"User Examples","text":"BlockDotProduct computes every lane product and the accumulation exactly, then projects once:\n\nrng = Xoshiro(9)\na64, b64 = randn(rng, 32), randn(rng, 32)\nqb(v) = ConvertToBlockMaxAbsFinite(Binary8p1uf, Binary8p4se, RNE_SatNone, RNE_SatNone,\n            ntuple(i -> Convert(Binary8p4se, RNE_SatFinite, v[i]), Val(32)))\ndq = BlockDotProduct(Binary8p4se, RNE_SatNone, qb(a64), qb(b64))\n(a64'b64, decode(dq))\n\n(5.0634, 4.5)       # difference is input quantization only, never accumulation error","title":"Quantized dot products with one final rounding"},{"category":"section","location":"user_examples/#Why-training-loops-like-stochastic-rounding:-the-swamping-demo","page":"User Examples","text":"Accumulate 400 gradient steps of 0.011 into a Binary8p3se accumulator. Under nearest rounding, the moment the accumulator dwarfs the increment, every add rounds back to the accumulator — it stalls. Stochastic rounding keeps absorbing the increments in expectation:\n\nfunction accumulate_demo(nsteps, g)\n    σ = RSA_SatNone(16)\n    rng = Xoshiro(11)\n    acc_rne = Binary8p3se(0.0); acc_sto = Binary8p3se(0.0)\n    for _ in 1:nsteps\n        acc_rne = Add(Binary8p3se, RNE_SatNone, acc_rne, Convert(Binary8p3se, RNE_SatNone, g))\n        acc_sto = Add(Binary8p3se, σ, acc_sto, Convert(Binary8p3se, σ, g; rng); rng)\n    end\n    (nsteps * g, decode(acc_rne), decode(acc_sto))\nend\naccumulate_demo(400, 0.011)\n\n(4.4, 0.125, 5.0)   # exact sum, RNE accumulator (stalled!), stochastic accumulator\n\nThe stochastic result is noisy (5.0 vs 4.4 on this seed) but unbiased; the RNE result is wrong by 35× and no amount of steps will fix it. In practice you keep a wider accumulator when you can — and use stochastic rounding when you can't.","title":"Why training loops like stochastic rounding: the swamping demo"},{"category":"section","location":"user_examples/#Activation-functions-are-256-byte-lookup-tables","page":"User Examples","text":"For pure specs, a unary activation over an 8-bit format is a finite function — the whole nonlinearity is one 256-byte table, built bit-exactly through the scalar path and then gathered per element. This is precisely the activation LUT you would burn into an accelerator:\n\nusing ByteFloats, Random\nempty_tables!()\nrng = Xoshiro(4)\npre = Binary8p4se.(randn(rng, 4096) .* 2.5)       # pre-activations, ±2.5σ\nact = Tanh(Binary8p4se, RNE_SatNone, pre)          # table-gather kernel\n\n(table_bytes(),\n round(count(v -> abs(decode(v)) == 1.0, act) / 4096; digits=3),\n length(unique(codepoint.(act))),\n decode(NextLessThan(one(Binary8p4se))))\n\n(256, 0.384, 100, 0.9375)\n# LUT size; fraction saturated to ±1; distinct output codes; last value below 1\n\nTwo things worth staring at. First, saturation is severe at this input scale: 38% of pre-activations land exactly on ±1 — under RNE, tanh reaches 1 as soon as the true value rounds past the midpoint of the last gap, and the entire saturated tail becomes indistinguishable to the next layer. (Under TowardNegative the asymptote is honored instead: tanh of any finite positive input projects to 0.9375, never 1 — the projection mode is part of the activation design space.) Second, the choice of nonlinearity changes information retention in code points, not just shape:\n\nactS = Softplus(Binary8p4se, RNE_SatNone, pre)\n(length(unique(codepoint.(actS))), table_bytes())\n\n(61, 512)          # softplus keeps 61 distinct codes here; two LUTs now cached\n\nAuditing an activation for a format is this cheap: enumerate all 256 inputs, look at the output histogram, count what survives.","title":"Activation functions are 256-byte lookup tables"},{"category":"section","location":"user_examples/#Packing-a-quantized-model","page":"User Examples","text":"n = 1_000_000\nmodel = [rawvalue(Binary5p2se, UInt8(rand(0:31))) for _ in 1:n]\npv = PackedVector(model)\n(sizeof(model), sizeof(pv.data))\n\n(1000000, 625000)   # 8 bits → 5 bits per value; indexing and vmap work directly on pv","title":"Packing a quantized model"},{"category":"section","location":"#ByteFloats.jl","page":"Home","text":"A conforming, performance-oriented Julia implementation of the IEEE P3109 draft standard — arithmetic formats for machine learning at bitwidths 3–8.\n\nBit-exact defined results on every default path; one projection engine as the single write path into a code point; approximation only behind an explicitly named, exhaustively measured κ registry; ≈ 8.9 million test assertions; table-gather kernels at fractions of a nanosecond per element.\n\njulia> using ByteFloats\n\njulia> Binary8p4se(1.6) + Binary8p4se(0.25)\nBinary8p4se(1.875 ≡ 0x47)\n\njulia> Binary5p3sf(0x08) == Binary5p3sf(1.0)     # UInt8 constructs from a code point\ntrue","title":"ByteFloats.jl"},{"category":"section","location":"#Documentation-map","page":"Home","text":"Introduction — what the package is, the design pillars, a thirty-second tour, installation.\nCheat Sheet — one-page lookup for format names, conversion, projections, operations, arrays, blocks, packed storage, and common traps.\nUser Guide — the complete public API in usage order: formats, values, projection specifications, the two operation registers, arrays and sorting, blocks, packed storage, conformance and κ, performance guidance.\nUser Examples — runnable examples in three tiers: basic, machine learning, deep learning.\nJulia Compatibility — the Base register: which Base functions work on Binary values, what they map to, and what deliberately isn't mapped.\nTechnical Guide — internals: the encoding and projection engine, the oracle's rigor classes, tables and kernels, the block layer's exactness filters, the verification and benchmark doctrines.\nTechnical Examples — internals-level recipes: pipeline introspection, exhaustive verification of custom code, κ measurement, doctrine-compliant benchmarking.\nAdding Operations — how to add new scalar (unary through quaternary) and block (scaled, elementwise, reductive) operations with every guarantee intact: registry mechanics, ω-semantics duties, worked examples.\nExternal Reference — docstrings for the documented public surface.\nInternal Reference — docstrings for the documented unexported machinery.","title":"Documentation map"}]
}
